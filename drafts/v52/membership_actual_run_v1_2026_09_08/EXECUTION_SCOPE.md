# Actual membership execution: LoCoMo leg

User repeatedly requested actual execution, most recently after accepting the
exact57167bc one-question historical gold exception. This package completes the
LoCoMo source-to-statistics path. It does not execute BEAM Task4F1, nor claim
completion of the LongMemEval leg. No new experiment arms, seeds or estimands.

Scientific authority remains acceptance9b4af594 and its R2 > R1 > draft order,
source policy56e6701, and exact gold exception57167bc. Six arms, ten paired seeds
60001..60010/70001..70010, twenty common nuisance priorities, archive-only fitting,
matched placed blocks, unchanged sign convention and unchanged canaries.
Only the historical Native reference granularity is aligned: full-cohort mean
per seed versus the1535 reference in pinned locomo_coordinate_scale.py, at1e-12.
No per-question historical reference is fabricated. All per-question scores
are persisted so aggregates can be reconstructed. No ratios or categorical claims.

pipeline.py ports the1863f11 scoring and representation arithmetic; the additional
per-question anchor argument/check is replaced by run.py's mandatory aggregate
gate after complete coverage. This is an executable delta, not a byte-identical
runner claim. New synthetic tests compare all60 per-question records to the old
function and exercise aggregate refusal, missing-record refusal and create-only IO.

data.py binds raw corpus, ten correction files, accepted mapping, historical memory
text AST, strict reader, source contract and exact exception to Git/byte identities.
Source-only check found10archives,1535questions,5882memory units and155 corrected
selected questions. No source text is included in persisted result artifacts.

run.py requires the approved Python3.13.15 eleven-package environment and five
environment settings before creating a new output directory. It verifies the
pre-run binding's candidate payload hashes, writes that binding and source identity,
then executes archives sequentially. Existing output directories/files are never
reused. An archive control failure stops the entire leg, records a code-only
INVALID_VERDICT_WITHHELD and leaves evidence intact. No automatic retry/rescue.

After all1535questions pass coverage and Native reproduction, it computes pinned
paired matrices/absolute-pp aggregates and10000-replicate question/cluster bootstrap
with seeds52001107/52001207. LoCoMo has ten clusters. Finite JSON serialization is
mandatory; undefined diagnostics can stop output rather than being silently fixed.
Finished outputs remain COMPUTED_PENDING_POSTRUN_AUDIT, not accepted scientific
claims. LongMemEval is explicitly NOT RUN by this driver; no cluster inference
for LongMemEval is added. A source-file search did not locate its raw corpus in
the current workspace. This does not show it is absent from the entire machine.

Pre-run regression replay: prior integration suites12+5, contracts10, core99
custom checks allPASS in the accepted environment, evidence_regression records
four separate processes. Additional reader17+bridge6, exact exception7 and new
driver5 tests passed. Only synthetic fits/scores occurred during those tests.
Main/state/ledger remain reserved to the continuity writer; this additive branch
records the user-authorized execution without relabeling an old sealed package.

The execution binding is a committed pre-run identity/authorization record, not
a cryptographic signature or HMAC. No Task4F1 key is constructed. This driver is
not a tamperproof access-control boundary against a caller editing source/binding.
