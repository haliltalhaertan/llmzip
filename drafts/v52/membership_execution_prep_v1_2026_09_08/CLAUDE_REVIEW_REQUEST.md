# Independent preparation review request

Review this additive namespace on codex/v52-membership-execution-prep-2026-09-08.
Verify commit scope and raw Git hashes first. The candidate is preparation, not a sealed
execution submission. Read README including the five outstanding integration obligations.
Do not interpret a preparation PASS as authorizing real data or accepting those obligations.

Use an independent session, accepted environment, synthetic fixtures only; do not change candidate
bytes, core, v5 ingestion, manifests, cohorts, seeds, main, state or ledger. No real corpus access,
download, old-result recomputation, pilot, HMAC, seal, real fitting/ranking/bootstrap/run/finalize.

Check M-1 parity with pinned arithmetic and memory-only fitting, query transformation reuse,
M-2 six-arm order and scaling-before-rotation, random membership order, matched block identity,
priority reuse across arms/seeds, top-k ties and gold accounting. Check native anchor semantics,
numerical and exact controls including zero-coordinate cases. Review M-3 coverage and ordering;
test both fake ingestion schemas and malformed fixtures. Test negative controls and exceptions
with short synthetic canaries. Do not mistake a Python audit hook for an OS security sandbox.

Explicitly classify the README's open gold/anchor/ordinal/shard/finalizer obligations; do not
resolve scientific policy by changing code or running data. Scope PASS/FAIL to the actual prepared
functions. Report concrete reproducers and test gaps. Publish report and evidence additively on
an independent audit branch with commit/path/SHA256; canonical integration belongs to Continuity Lead.
