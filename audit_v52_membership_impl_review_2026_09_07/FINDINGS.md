# Findings — V52 Membership Under Scaling, independent implementation review

Candidate: `impl/v52-membership-under-scaling-2026-09-07` @ `a6d70ff63ce44ea0f066a075853c9e93dec53122`, directory `drafts/v52/membership_impl_2026_09_07/`.
Governing: `docs/v52/V52_MEMBERSHIP_UNDER_SCALING_ACCEPTANCE_BINDING_2026-09-07.md` @ `9b4af59` (precedence R2 -> R1 -> draft).

Severity key — **BLOCKER**: must be fixed before this core produces any record. **REAL**: a true finding that does not block. **COSMETIC**: hardening or tidiness.

| id | severity | area | location | finding | evidence | suggested fix |
|---|---|---|---|---|---|---|
| F-1 | **BLOCKER** | C | `membership_scaling_core.py` L91 | `cv_sigma_after` is the **standard deviation** of the post-scaling sigma, not its coefficient of variation — it is never divided by the mean. `cv_sigma_before` (L83) *is* a genuine CV. R1 section 1 requires CV(sigma) before **and** after; R1 section 4 requires every diagnostic to be published unconditionally, so a mislabelled number would enter the record. The two agree only when every post-scaling sigma is 1, i.e. when no coordinate took the eps fallback — precisely the case the diagnostic exists to describe. | 20 dead coordinates: reported `0.406116`, SD `0.406116`, true CV `0.512989`. Mutation **M10** (report a true CV) is missed by the whole preparer suite. `evidence/adversarial_core_output.txt`, `evidence/mutation_probe_output.txt` | divide by `post_sigma.mean()`, or rename the key `sd_sigma_after` and say so |
| F-2 | **BLOCKER** | D / E | `membership_scaling_core.py` L294-298 | Cluster labels are validated only for **length**. A NaN label becomes a cluster whose member set is **empty** (`labels == nan` is false everywhere), so any replicate drawing it silently omits those questions from numerator *and* slot denominator. Mixed-type labels (1 vs "1") split one conversation into two clusters. When every draw lands on the empty cluster the failure surfaces as a misleading "empty question selection". This is the silent acceptance that acceptance record section 7 bars. | a draw of 3 clusters covering all 10 questions produced `n_question_slots = 8`; `n_clusters = 2` for a 10-element mixed-type label vector. `evidence/adversarial_core_output.txt` area D | reject non-finite, null and mixed-type labels; require every cluster non-empty; keep the length check |
| F-3 | REAL | E | `membership_scaling_core.py` L179, L219 | Duplicate question ids pass `validate_per_question_records` unnoticed — the `expected` set comprehension de-duplicates them, and `qi` is a dict. The result is **never silently wrong** (every variant tested aborts, because the max index reaches or exceeds the dict length), but it aborts with an unhandled `IndexError`, not a `DesignViolation` naming the cause. | three duplicate-id variants, all IndexError. `evidence/adversarial_core_output.txt` area E | add an explicit duplicate-id check to the validator |
| F-4 | REAL | F | `test_membership_scaling_core.py` L37-54 | The forbidden-token self-check is **not sound**. (a) Scope: the scan splits the source on the triple-quote and takes part 2, which excludes the module docstring — 1,321 chars, and the only region containing `indeterminate`, `ratio`, `relative scale unsuitable`, `verdict`. (b) Power: a token scan cannot see a forbidden quantity spelled differently. | Mutation **M1** adds a real Delta/G ratio to the returned schema as `relative_change`; all 74 preparer checks still pass. `evidence/mutation_probe_output.txt` | replace with an AST check: enumerate every division and every output-schema key |
| F-5 | REAL | D / F | `test_membership_scaling_core.py` L188-201; `README_IMPL.md` | The cluster-multiplicity fixture (3 questions worth 1.0, 5 worth 0.0) yields **identical** values under slot-weighting and under distinct-question weighting, so it cannot demonstrate the bound denominator. The README nevertheless presents it as demonstrating exactly that. | Mutation **M2** (divide by distinct questions) and **M3** (cluster-average inside `cluster_bootstrap`) both pass the preparer suite. A third cluster of different size and value separates them (CCB: slots 1.7778, distinct 1.1429, clusters 2.6667). `evidence/mutation_probe_output.txt` | adopt a three-cluster fixture with unequal sizes and values, plus an end-to-end reference comparison |
| F-6 | REAL | E | `membership_scaling_core.py` L45, L52 | `require_real_data_authorization` is **declarative only**: no function in the core calls it (AST-verified), and its default argument is bound at import, so assigning `REAL_DATA_EXECUTION_ENABLED = True` on the module does **not** open the gate. The direction of that surprise is fail-closed and the core genuinely has no corpus-touching path, so nothing is unsafe today — but the gate as written enforces nothing, and the module constant is not the live switch a reader assumes. | `callers=[]`; flipping the constant still raises. `evidence/adversarial_core_output.txt` area E | read the module global at call time, and make the first act of the runner a call to this gate |
| F-7 | COSMETIC | E | `membership_scaling_core.py` L242 | `aggregate` silently accepts **negative** indices (they wrap: index -1 gives the same answer as index 9) and silently truncates float indices. Not reachable from either bootstrap. | `aggregate(idx=[-1]) = 18.5 = aggregate(idx=[9])`; a float index 0.7 accepted as row 0 | validate the index range and integer dtype |
| F-8 | COSMETIC | E | `membership_scaling_core.py` L245 | An out-of-range index raises `IndexError`, not `DesignViolation`. | index 99 out of bounds for axis 0 with size 10 | fold into the F-7 bounds check |
| F-9 | COSMETIC | E | `membership_scaling_core.py` L184 | A boolean score passes validation as `float(True) = 1.0`. | no violation raised on a record whose `fractional_R3` is True | reject bool explicitly |
| F-10 | COSMETIC | C | `membership_scaling_core.py` L75-79 | An **empty** (n=0) archive is accepted silently (sigma is NaN, so every coordinate takes the fallback, D = I, flagged True), as is a single-row archive. | n=0 accepted: `degenerate=96, flagged=True` | require at least two rows |
| F-11 | COSMETIC | F | `test_membership_scaling_core.py` L51, L109, L181 | Shipped sloppiness: `body.replace("block", "")` is a no-op with respect to the word it guards; `"distinct" if False else "partition"` is a dead conditional in a needle argument; and one check asserts membership in (True, False), which is true for every boolean and asserts nothing. | `evidence/preparer_selfcheck_output.txt` | delete the dead conditional; assert `spans_zero is True` on a fixture that spans zero |
| F-12 | COSMETIC | A | `membership_scaling_core.py` L39, L251 | The linearity consistency tolerance is **absolute** at 1e-12. That is what binding section 6 says and it is honoured literally, but measured headroom under an adversarial worst case (gaps of +/-100 pp, 4,000 slots, 400 replicates) is only about **19.5x**. | worst absolute difference between mean-of-differences and difference-of-means = 5.116e-14 | record the headroom; consider a relative tolerance only via a new decision, never silently |

## Non-defects, recorded so they are not re-discovered

| # | observation |
|---|---|
| N-1 | Only four arms enter `G` and `G_scaled`; `NATIVE` and `SCALED_NATIVE` are rotation-independent controls. The "all six arms" clause of binding section 4 step 6 is satisfied vacuously for them. The validator does require records for all six arms at all ten seeds, matching the R1 section 2 schema of n_questions x 10 x 6. |
| N-2 | No bootstrap RNG seed is a module constant; it is a caller argument. Reproducibility of the published interval therefore rests on the runner, and the seed should be preregistered. |
| N-3 | The mandatory LONGMEMEVAL SINGLE-COMPONENT inheritance tag is not emitted anywhere, and nothing stops a caller from cluster-bootstrapping LongMemEval. Both are runner obligations under R2 section 4, not core defects. |
| N-4 | Nothing applies D to a query outside `check_identity`. The runner must apply the identical D and estimate nothing from the query. |
| N-5 | sigma exactly equal to eps takes the **divide** branch (d = 1e12), because R1 section 1 makes the fallback apply when sigma is strictly below eps. Design-faithful, and a razor edge worth knowing. |
| N-6 | The module docstring names several forbidden items in order to declare them absent. Judged acceptable — naming a thing to forbid it is not implementing it — but it is also exactly the region the token scan excludes (see F-4). |
| N-7 | `DEGENERATE_FLAG_THRESHOLD` is read in exactly one place, to set `flagged`, and `flagged` is consumed by nothing. It is a diagnostic, not a decision input, as R1 section 1 requires. The self-disclosed dead-code fix in the README is real and the flag now has correct "greater than 4" semantics — confirmed at the boundary that was not tested (counts 3, 4 and 5). |
| N-8 | The impl branch carries exactly one commit adding exactly four files under `drafts/v52/membership_impl_2026_09_07/`. No workflow, no `.github/` path, no seal, no Task 4F1 namespace, no `research/v52/` path. |
| N-9 | The shipped `synthetic_test_output.txt` is reproducible: 74/74 pass and the output is byte-identical except for the one line printing a random temp path. |