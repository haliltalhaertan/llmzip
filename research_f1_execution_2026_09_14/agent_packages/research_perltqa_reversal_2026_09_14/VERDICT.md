[LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]

# VERDICT — is the mechanism question any closer? (PREPARED, NOT ACCEPTED)

## Short answer

Slightly closer by elimination, not by discovery. The PerLTQA reversal numbers are now independently
re-derived from the same bytes (all four section deltas, the -6.275 composition, the 30/30-vs-23/30
within-archive flip — CONFIRMED, `REVERSAL_FACTS.md`). The most testable ranking-geometry candidate,
native-Hamming boundary-competition density (H1), is dead as the regime explanation: it points the
wrong way on PerLTQA section levels, on LME question-type levels, and is non-monotone on REALTALK
categories, while its per-query rules lose to the majority prior on all three testable benchmarks
(`CROSS_BENCHMARK_TEST.md`). The mechanism question is closer in the precise sense that one more
serious candidate is excluded with first-hand numbers — including the informative asymmetry that
competition modulates loss depth inside losing regimes (events) while saying nothing about the winning
regime's intercept (profile).

## The assumption that would most damage this conclusion — and its test

Most damaging assumption: the committed per-q caches misrepresent the frozen experiment (wrong K/NT,
wrong arms, or E1 post-processed the numbers), making both our confirmation and our kill artefacts.
Test applied: (1) byte identity — SHA256 of our PerLTQA input matches E1's claimed hash exactly, and
REALTAK details hash matches too; (2) method read — `step2_eval.py` on the same branch derives every
field we used (K=3, NT=20, Hamming `tie`/`gap`, arm definitions) from first principles we re-implemented
independently; (3) second-route replication — our code reproduces E1's character-level tie-vs-Delta rhos
(profile +0.2475, events -0.3486), P64 same-sign fractions (68.9/86.4/77.1%), and all three frozen
headlines (LME +10.037943262411346 to 15 decimals) without copying E1's numbers. A cache that is wrong
in a way surviving all three checks is possible but contrived; the kill stands or falls with the caches.

## What remains unknown (honest inventory)

1. WHY profile queries win (+20.44) with the densest native boundaries — the intercept question.
   Unmeasured: per-section competition-gap LEVELS, Q-magnitude distributions by section, query-axis
   alignment. All need the pending frozen V2 per-query rows.
2. Whether the within-events modulation (gap rho +0.15 query / +0.44 character) is causal or a second
   marker — needs the preregistered fifth benchmark E1's checkpoint calls for, not more post-hoc slices.
3. LoCoMo per-query structure in committed form — absent; R2's Drive-backed numbers are CLAIM-only here.

## Next decisive measurement (single recommendation)

Persist the corrected R2 per-query competition rows (per-gold strictly-closer-rival and gold-distance
tie-mass gaps) with hashes/manifests, then report their LEVELS by PerLTQA section — not just slopes:
do events queries have larger or smaller strictly-closer-rival gaps than profile queries on the same
archive? H1's corpse predicts "larger" (denser→loses); the section means of the native gap predict
"smaller" (events native gap 1.71 > profile 1.17 already points that way). Whichever direction the
frozen rows show, one of the two surviving pictures dies. Until those rows land, no new codec race or
E2 promotion is justified — same governance as E1's checkpoint §9, independently reached.
