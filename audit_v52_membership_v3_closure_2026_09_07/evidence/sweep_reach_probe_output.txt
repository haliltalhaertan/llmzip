====================================================================================================
PART 1 - reach of the preparer's own sweep predicate
====================================================================================================
  FLAGGED      check('x', a > 0 or True)                 the exact v2 defect: literal True LAST
  FLAGGED      check('x', True or a > 0)                 literal True FIRST - outside a literal reading of 'ending in'
  FLAGGED      check('x', a > 0 and True)                an `and True` conjunct
  FLAGGED      check('x', a > 0 or b > 0 or True)        three-way, True last
  not flagged  check('x', True)                          bare literal True - the exempted did-not-raise marker
  not flagged  check('x', a > 0 or (1 == 1))             unconditionally true, but NOT a literal True
  not flagged  check('x', a > 0 or bool(1))              unconditionally true via a call
  not flagged  check('x', a > 0 or 1)                    truthy literal that is not `True`
  not flagged  check('x', not False)                     constant via a unary not
  not flagged  check('x', a > 0)                         an honest condition (control - must NOT be flagged)

Reading: the code flags a BoolOp with a literal True in ANY position, so it is STRONGER than
the phrase 'ending in a literal True'. It is weaker than 'all unconditionally true
conditions', which the preparer explicitly does not claim.

====================================================================================================
PART 2 - does the NEW-2 kill rest on a single check?
====================================================================================================

  core mutant only
    exit code 1 -> KILLED
      FAIL: F-1: cv_sigma_before is a genuine CV of this archive   123456.000000000
      FAIL: F-1: a zero-mean sigma gives NaN, explicitly and with no epsilon added

  core mutant + the replacement check neutered with `or (1 == 1)`
    exit code 1 -> KILLED
      FAIL: F-1: a zero-mean sigma gives NaN, explicitly and with no epsilon added

  test neutered only, core untouched (control)
    exit code 0 -> *** SURVIVES ***
      (no named FAIL lines)
