# V52 Boundary Localization — Independent Cold-Start Audit: Gate Table A–S

Audited commit: `1a33ef0d1a2715257920201f7a3db8ab4677a007`
Branch: `research/v52-sign-mechanism-locomo-2026-09-04` (branch head == audited commit; the branch had not moved)
Auditor: cold-start, session-independent. No prior session's verdict, gate result or number was supplied or assumed.

`established` = the auditor positively demonstrated the property from bytes or from computation.
`not falsified` = the auditor looked for a violation and found none, but the property is not positively demonstrable with the artifacts in this package.

| Gate | Verdict | Strength | Evidence |
|---|---|---|---|
| **A** Preregistration precedes outcome access | **PASS** | established | `evidence/` + DAG: strictly linear chain `8858ecf`(addendum) → `20037d7`(prereg) → `e14cf8a`/`08b3b1d`(runners) → `6be2a53`/`6648d46`(workflows) → `71343b8`(seal) → `2edeef4`(trigger) → `89e38d1`/`cb4295c`(outputs) → `5e5ccb3`(checkpoint) → `1a33ef0`(manifest). `git merge-base --is-ancestor` confirms every forward edge and refutes every reverse edge. Zero files under either `*_boundary_outputs/` exist in the tree at `8858ecf`, `20037d7`, `71343b8` or `2edeef4`. The trigger blob is absent at the seal commit, as the seal asserts. The seal's `package_head_before_seal` (`6648d46`) equals the actual first parent of the seal commit — a binding not under narrative control. Both result summaries name the prereg blob `b8aceb…`. No result could have existed before the preregistration bytes did. |
| **B** Frozen boundaries exactly {16,24,32,48,64} | **PASS** | established | `BOUNDARIES = [16, 24, 32, 48, 64]` in both runners (`locomo_boundary_localization.py:18`, `longmemeval_boundary_localization_shard.py:19`). Per-question files contain exactly six arms with perfectly balanced counts (LoCoMo 15350 each, LME 4700 each). No boundary literal outside the grid appears in either runner. Nothing was computed and discarded. |
| **C** Seeds exactly 58001–58010 / 68001–68010 | **PASS** | established | Declared in both runners, the seal, and both summaries; independently confirmed present in the per-question records; partition seeds independently regenerated from the seed integers alone. Pairing rule `5800i ↔ 6800i` verified on every RANDOM32 row. |
| **D** No unreported boundaries, runs, retries or tuning | **PASS** | established | GitHub Actions API: each workflow has `total_count = 1`, `run_number = 1`, `run_attempt = 1`, `event = push`, `head_sha = 2edeef4` (the trigger commit), conclusion `success`. LongMemEval's 11 jobs (shards 0–9 + aggregate) are each `run_attempt = 1`; the pre-run gate step succeeded in every job. Neither workflow declares `workflow_dispatch`, `repository_dispatch` or `schedule`, so no manual re-trigger path exists; the only trigger is a push touching the single trigger file. Exactly two persist commits exist. `curl --retry 5` is a dataset-download retry, not an experiment retry. Run ids match those declared in the checkpoint. `evidence/actions_run_history.json` |
| **E** Exact frozen source identities | **PASS** | established | Base modules declare LoCoMo dataset `79fa87e9…`, audit manifest `90a4e94c…`; LongMemEval dataset `d6f21ea9…`, bytes `277383467`, adapters `0a1a39a8…`/`643082d6…`, `EXPECTED_PRIMARY = 470`; `TOPK = 3`, `N_NUISANCE = 20` — all identical to preregistration §3. Base blobs `7c414025…`/`79dd4a5e…` match the seal. Cohorts 1535/470 enforced by hard `raise` in the runners and confirmed in the persisted records. LongMemEval dataset bytes and SHA-256 are re-verified inside CI before computation. |
| **F** Native reproduction | **PASS** | established | Reconstructed from the persisted per-question native contributions: LoCoMo `|Δ| = 1.11e-16`, LongMemEval `|Δ| = 0.0` against the frozen anchors. (The package's own declared errors are the mirror pair — LoCoMo `0.0`, LME `1.11e-16` — the difference being summation order; both are at the float64 floor and far inside the preregistered `1e-12`.) `native_fractional_R3` is constant per question across all arms and seeds, as it must be. |
| **G** Spectral transformation construction | **PASS** | established | Construction retyped by the auditor from preregistration §5 prose and rebuilt in isolation with numpy: single RNG stream per seed, head block drawn first, QR with deterministic `diag(R)<0` sign correction, blocks placed leading/trailing. Worst `‖RᵀR − I‖∞ = 1.55e-15` over all 50 (seed, boundary) matrices — three orders inside the `1e-12` tolerance. Off-block entries exactly `0.0`. Applied to archive and query alike (`C @ R`, `QC @ R`) after archive-mean centering (`C = Y − mu`, `QC = QY − mu`) and before sign quantization, which remains `>= 0`. `scripts/verify_transforms_and_partitions.py` |
| **H** Matched Q32/Q64 equality | **PASS** | established | Re-derived independently: max abs error `0.0` exactly, confirming the declared `matched_q32_q64_max_abs_error = 0.0`. Strict test additionally confirms the RANDOM32 matrix *places* the same numeric Q32/Q64 (block extraction error `0.0`, entries outside the two blocks exactly `0.0`, orthogonality `1.78e-15`) while genuinely differing from the spectral arm (min `‖R_rand − R_spec‖∞ = 0.667`), and that no partition is trivially the leading 32 coordinates (overlap 6–14 of 32, consistent with the hypergeometric mean 10.67). `scripts/verify_matched_arm_strict.py` |
| **I** Random partition validity | **PASS** | established | All ten partitions regenerated **from the seed integers alone** and matched byte-for-byte, including array order, against *both* persisted files. Cardinality 32/64, uniqueness, disjointness, exhaustiveness over 0..95, range and seed-pairing verified for all ten. |
| **J** Per-question completeness and duplication | **PASS** | established | LoCoMo 92100/92100 rows, 1535 unique questions; LongMemEval 28200/28200 rows, 470 unique. Exactly one record per (question, arm, seed) — min = max = 1 over all cells; cell count equals the expected product. `partition_seed` populated exactly on RANDOM32 and empty elsewhere. |
| **K** Rebuild every seed-level R@3 and rho from per-question outputs | **PASS** | established | **Rebuilt from the per-question CSVs only** — the summary JSONs and the per-seed CSVs were deliberately excluded as inputs, then used solely as comparison targets. All 120 seed-level R@3 and rho, and every `mean_R3`, `rho`, `sample_sd_R3`, `se_R3`, `sample_sd_rho`, `se_rho`, `rho_min`, `rho_max`, reproduce with **worst absolute deviation 2.04e-15** across both datasets (float64 summation-order noise). The per-seed CSVs agree to ≤ 2.22e-16. `scripts/reconstruct_from_per_question.py`, `scripts/compare_to_declared.py` |
| **L** Reconstruct S_LoCoMo, S_LongMemEval, S_common | **PASS** | established | From the auditor's own reconstruction: `S_LoCoMo = {32}`, `S_LongMemEval = {32,48}`, `S_common = {32}`. |
| **M** Mechanical verdict follows | **PASS** | established | Applying preregistration §10–§11 to the auditor's reconstruction: P1 passes on both datasets; P2 = `SPECTRAL_POSITION_CONFIRMED` on both (Δ32 = 0.8548 / 0.8290, both ≥ 0.25); `S_common == {32}` fires **L1** `[PC32-LOCALIZED SUFFICIENCY LEAD — ON TESTED GRID]`; `S_LoCoMo ≠ S_LongMemEval` fires `[BOUNDARY-SET HETEROGENEITY PRESENT]`. Threshold 0.25, the seed panel, the grid and the estimand are byte-identical between the preregistration (committed at `20037d7`) and the runners; no post-outcome alteration is possible without rewriting sealed history. |
| **N** LongMemEval sharding semantics | **PASS** | established | Shard partition is `i % nshards == idx` over the 470-item primary cohort — disjoint and exhaustive by construction; the aggregator hard-requires shard indices exactly `range(10)`. The tie-break RNG seed is `5100000 + lex*100000 + t*100 + 99` where `lex` is the **global** lexicographic rank over all 500 dataset question ids (`allq = sorted(...)` before sharding), i.e. shard-invariant. Confirmed empirically from the persisted `tie_identity` strings: 470 unique lex values, strictly increasing in sorted-qid order, range 0–498 with exactly 30 unused slots (the 30 `_abs` items), max 498 ≫ any shard size. Aggregation sorts globally by qid before computing means. The claim `DETERMINISTIC_SHARDED_SEMANTICS_INVARIANT` is exactly what is demonstrable; the package correctly declines the stronger bitwise-exactness claim. `scripts/verify_shard_invariance.py` |
| **O** Uncertainty and dispersion disclosures | **PASS** *(with a scope caveat carried to S)* | established | All 120 per-seed rho values printed in checkpoint §3 match the auditor's reconstruction **exactly at the 6 dp printed** (worst deviation 0.0). All `sample_sd`, `se`, `rho_min`, `rho_max` verified to ≤ 2.04e-15. `sign_inverting_rotation_seeds` is empty for every arm and that is *correct* — no seed had `R_b > R_native`. The two individually threshold-exceeding seeds are named in the checkpoint as required: LongMemEval **B32 seed 58004** (ρ≈0.267) and **B48 seed 58009** (ρ≈0.302); the auditor's independent scan returns exactly and only those two among the sufficient arms. **Scope caveat (not a failure):** the disclosed SE is the *seed-panel* component only, conditional on the frozen denominator, which the package states. It is not the full sampling uncertainty of ρ — see Gate S. |
| **P** Historical disclosure | **PASS** | established | LoCoMo seed `56001` remains disclosed in the binding post-audit addendum §3 (`R@3 = 0.24319099434005997`, `rho_2 ≈ -0.0672`), in the historical cross-benchmark checkpoint, and in `locomo_head_tail_summary.json`. The auditor recomputed it: `L_2 = -0.00664384767564943` (negative ⇒ genuine sign inversion), `rho_2 = -0.067219`. The new checkpoint explicitly carries it forward as "a binding historical disclosure … not erased by this new panel" rather than dropping it now that the new panel shows no inversions. |
| **Q** Interpretation ceiling | **PASS** | established | Both mandatory labels appear, and appear **together**, in every artifact where the L1 label occurs (checkpoint, provenance manifest, preregistration) — verified by co-occurrence count. Sufficiency-≠-necessity, coarse-grid-≠-global-optimum, shared-pipeline-≠-independent-replication and the no-Task-4F1-transfer statements are all present (prereg §18, checkpoint §7, both summary `interpretation_ceiling` fields). The checkpoint volunteers the LongMemEval 48/48 admission and the unquantified Full-Haar denominator against its own headline. |
| **R** Task 4F1 boundary | **PASS** | established | Zero occurrences of `4F1`, `BEAM`, `HMAC`, `run_archives`, `evaluate_archive`, `finalize_results` or `AUTH_HMAC` in either runner or either workflow. Every occurrence anywhere in the stage is an explicit *exclusion* (prereg §3/§18, seal `task4f1_execution_touched: false`, checkpoint §6/§7, manifest). No commit in the stage (`8858ecf^..1a33ef0`, 20 paths) touches any 4F1 path. **Auditor's own conduct:** no Task 4F1 candidate was invoked in any mode; `run_archives`, `evaluate_archive`, `finalize_results` were never called; `V52_T4F1_AUTH_HMAC_KEY_HEX` was never set; no authorization was constructed; no Task 4F1 retrieval outcome was accessed or sought. |
| **S** Strongest argument against | **COMPLETED — the case WEAKENS the result; it does not defeat it** | see report §5 | Quantified in `scripts/gate_s_adversarial.py` and `scripts/gate_s_bootstrap_paired.py`. The position-dependence core is unassailable (RANDOM32 at −50 / −32 SE, bootstrap P(sufficient) = 0.0000 on both datasets). The *uniqueness* claim is not: under a paired question-level bootstrap, `S_common = {32}` holds in only **72.95%** of resamples ({32,48} 16.57%, ∅ 10.17%). The reported conditional SE (0.012–0.030) omits the question-sampling component, which is 3–5× larger (bootstrap SE 0.063–0.088). The single load-bearing exclusion — LoCoMo B48 ∉ S_LoCoMo — has P(sufficient) = 0.21. See report §5 for the full argument and the narrowed claim. |

## Summary

19 gates. **A–R: PASS** (all established, none resting on "not falsified"). **S: completed**, and it materially narrows the claim that may be reported.

No gate is `NOT ESTABLISHED`. No gate is `FAIL`.
