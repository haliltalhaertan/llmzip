Continue llmzip from user-supplied published representation-geometry handoff. Base commit EXACT 75b7e057abbda4c46aabe3a7fb0d51f98b245ad0; initial branch findings/representation-geometry-2026-09-13. Work only in CURRENT isolated worktree on your assigned Muse branch. No network/API/installs or push. Do not touch main, ledger, ops/CURRENT_STATE.json, frozen round4 (1021083), or ANY existing file under research_representation_geometry_2026_09_13/. The published flawed package MUST remain byte-identical with its REQUEST_CHANGES audit. All new work additive outside it. Task4F1 seal: no corpus, queries, gold/evidence, recall/ranking-accuracy/benchmark, embeddings or refit; only synthetic rational certificate tests. Old scripts may contain absolute paths to other workspaces and original benchmarks: INSPECT BEFORE RUNNING, do not execute old full suites blindly or modify their source to relocate. Path adaptation only on copied code, with provenance.

Read BOTH audit reports under research_representation_geometry_2026_09_13/AUDIT/ and especially BULGU-1/BULGU-5. Measurement review PASS WITH OBSERVATIONS, repairs REQUEST_CHANGES. No different-family independence claim: we are separate Muse sessions of same family. Original measurements are declared refits,15/470 archives,one seed; variance loss is NOT retrieval harm. Projector/gate observations remain open; no measurement extension in this task.

Coordinator verified package manifest73/73 and reproduced blocker using independent Fraction comparator; receipt at /mnt/c/Users/MDP/dev/llmzip-work/representation_geometry_fix/COORDINATOR_REPRO.json.
POC exact inputs p=(-100,100,0,10000),q=(-200,200,70000,0),J=[1,4]. True comparisons {5/4:+1,3/2:+1,7/4:0,5/2:-1,4:-1}; v2 returns ISOLATED_TIES,direction=-1, ties[1,7/4] (WRONG). Unscaled p0=(-1,1,0,1),q0=(-2,2,7,0) returns directionVARIES. Enumeration budget skip changes behavior under score-preserving scaling. Original conservative certifier returns UNRESOLVED. Unknown must remain unknown: MORE RESOLUTION MUST NEVER MEAN UNSOUND CERTIFICATION.

Required discipline: STATUS.md immediately, bounded first pass~20min; persistent receipts from every phase. Exact Fraction/stdlib wherever possible, Python -B, thread1. Before-hash original published tree and protected files, after-hash compare. Reports start [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. No unsupported 'certified safe generally' from finite tests. Distinguish executed reproduction, proof argument, sample fuzz evidence, and incomplete cases. Final report must include paths/commit/test counts/remaining findings. Original USER POC returning exit0 documents existence of the BUG, not repair success; repair test must assert sound outcome instead.

ROLE: root-cause repair engineer. Own additive directory research_representation_geometry_followup/candidate/ only. Do not read other worker branch/output (cold comparison later).
1. Read exact certify_v2.py COMPLETELY, not only flagged lines, and fuzz_rates.validate_cert. Form explicit root-cause hypothesis. Identify a deterministic one-command RED regression that checks the POC must not return a false single-direction certificate. Run RED against archived v2 BEFORE implementing (failure must be the wrong claim, not import/fixture error). Save traceback/stdout.
2. Develop additive certify_v3.py with same public certify_pair/P4 surface, doc contract spelled out (status may be ISOLATED_TIES with directionVARIES as in v2). Fix exact-root partition/classification in BOTH isolator branches, not a single hardcoded POC path. Candidate roots must be sign-filtered; exact rational crossing vs tangent requires proven root-free adjacent cells or exact multiplicity plus actual-score sign handling. Account for exact roots coinciding with endpoint/numerator root, multiple roots, split-root brackets, numerator-negative reversal, zero polynomials, invalid intervals/budgets, and rational enumeration cap skips. Never classify an interval by one sample without a proof of no unhandled roots. Refuse ambiguous coverage (UNRESOLVED). Preserve valid abstract-domain API; separately label which examples are real-vector realizable.
3. RED->GREEN vertical tests. Acceptable POC behavior VARIES with correct per-cell evidence; conservative UNRESOLVED remains sound but is a fallback, not pretend resolution. Include input-scale invariance of truthful status/direction, positive score-preserving transforms (a,b)->k(a,b),(u,v)->k²(u,v), and swapping docs/inverting direction. Root bound/precision exhaustion must not produce false certainty. No monkeypatching validator to accept candidate.
4. BULGU-5 is coupled: implement new independent certificate validator or explicit semantics tests that reject the old counterfeit certificate. Finite17-point scans can be useful falsification but are NOT a proof validator; provide interval/root coverage validation or accurately label sample-level check. Don't trust generator's own direction or shared helper as oracle.
5. Preserve v2 and original untouched, receipts distinguish all3 outcomes. Test synthetic public entrypoint and root-free evidence plus bounded fuzz. Measure unresolved rate only SECONDARY to soundness. No requirement to drive unresolved to zero.
6. Report all other audit findings as OPEN/untouched, no blanket REQUEST_CHANGES closure. Write FIX_DISPOSITION.md with old/new mappings, raw receipts and manifest (exclude own exact path). Commit only own new directory locally (if author config missing inspect prior author, do not global config). No push. Final give SHA and exact runnable command for reviewer. A subsequent independent test task will target your candidate after completion.