Complete campaign disclosure repairs ONLY in /home/mdp/muse-work/campaign-label-fix, branch muse/complete-campaign-labels, base 84e75cc. No push, no main changes, no dataset edits. ~15 minute bounded task. No web/model APIs. Prior independent audit stdout is /home/mdp/muse-work/campaign-label-audit.log. Read it. Required exact standalone banner near beginning:
[LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]

Verified base: campaign MANIFEST.sha256 has 578 entries, all valid, all other files covered once; 579 files total. 181 md/txt docs. R1 basename regex report|analysis|result|summary|finding|audit|dispo|receipt|campaign|readme|overview yields 47 files, 26 missing exact banner. Some only have partial labels (prior fixer skipped any file containing NOT FOR CITATION). Report includes precise paths. One muse_pilot_review.md banner appears incidentally at line46. 00_READ_ME_FIRST.md has non-exact labels. Earlier user '13/29' was different undocumented scope; don't force same counts.

Deliverable:
1. Deterministic executable inventory/checker with explicit include/exclude reasons. Cover at least union R1 plus all narrative .md result/review/verification/analysis/certification/summary/disposition/inventory documents regardless of case/name; adding the banner to all copied .md in campaign is acceptable if you explicitly describe it as campaign distribution status, NOT changing historical event provenance. Include report-like .txt (receipt/result). Raw .log, machine tables/json/csv, code and raw numeric evidence must remain unchanged.
2. Prepend required banner as its own first nonempty line to missing/nonprominent narrative copies; preserve original bytes after the additive prefix and original newline style (use bytes, not Windows text newline conversion). Do NOT delete partial warnings or rewrite scientific assertions. Frozen originals outside campaign untouched. Historical receipt bodies/manifest-referenced inner artifacts may have original integrity meaning: retain original content byte-for-byte and explicitly distinguish distribution copy versus original receipt; if modifying a copied narrative breaks embedded manifests, list the affected embedded manifests/paths and preserve accessible pristine parent-commit provenance. Never blindly rehash historical experiment manifests to mask source changes. If immutable/signed artifact cannot be touched, flag it with exact reason instead of asserting universal coverage.
3. Add a concise campaign disclosure correction report documenting selection rule, missing-before/after, all modified paths, original-byte preservation check (strip only added prefix), and provenance: original at git 84e75cc, no measurement changed. Label your report too.
4. Refresh only campaign MANIFEST.sha256 (root-relative paths as existing). Verify hashes, uniqueness and complete coverage including new scripts/report, excluding manifest itself. Final checker must fail on a deliberately banner-removed temp copy (mutation control), not just inspect your hand-picked additions. Use isolated temp copies; don't mutate frozen data.
5. Verify diff net: only banner prefixes, new checker/report and campaign manifest; no semantic changes to originals, no raw numeric edits, no secrets. Run actual checker and retain JSON evidence. Ensure checker outputs persisted complete path lists and aggregate counts computed in Python.
6. Commit locally. Report SHA, exact coverage/counts, bytes preserved, manifest results, any embedded-manifest caveat; no inflated 'all research certified' wording. Do not claim external upload.
