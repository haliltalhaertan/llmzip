Integrate and verify the static-storage V10 repair. Work ONLY in /home/mdp/muse-work/static-integrated on muse/integrate-static-v10 (HEAD 35b5058 concurrency candidate). No network, no push, no main edits, no measurements or Task4F1 authorization. Preserve frozen V7/V8/V9 bytes. User authorizes this integration. Use Muse only, no external model APIs. Timebox ~20 minutes: persist partial evidence if blocked rather than expanding scope.

Sources available in SAME git object store and sibling worktrees:
1. Current HEAD 35b50581b566ff9f5dfbcfe5d7035f6f098d192e: concurrency gate consumption_gate_v10.py and tests under drafts/v52/static_storage_integration_v10_2026_09_13. Coordinator independently ran 13 pass; V52_LOADER_UNDER_TEST=v9 gives exactly two expected failures. Deterministic builtins.exec instrumentation.
2. 5ff0ab0368b1d1515b5214b7952757aeb03651c3 (/home/mdp/muse-work/static-denom): denominator_integrity_v10.py and test_denominator_integrity_v10.py. 18 tests pass. It currently WRAPS V9, not the hardened gate. It must instead flow through the hardened chain. Commit also uses same V10_DISPOSITION/HASHES/receipt names, so do NOT blind cherry-pick or overwrite the concurrency report. Copy two unique code/test files with git show or deliberate merge; preserve both historical candidate receipts under distinct provenance names.
3. 6e764b1207b565b16beb460151a4b955b69a94f0 (/home/mdp/muse-work/static-tests): additive directory drafts/v52/static_storage_security_regression_2026_09_13. Can cherry-pick this commit since directory distinct. Contains REAL valid 470-archive E2E fixtures, gate adapter, mutation variants and new tests. Read them before integration.

Acceptance requirements:
A. ONE public integrated preflight/context API, not separate unrelated helpers. Unique-by-archive logical denominator; rows carry archive_id, physical_copy_id, population_id, frozen N_i. Consistency/roster enforcement; sum bytes separately and NEVER dedup physical bytes. Original duplicate-copy test only repeats SAME physical_copy_id: that does NOT cover defect. Add a valid-plan E2E scenario with DISTINCT physical_copy_id values for same archive, valid format/config/item coverage, and independently frozen hashes/bindings; acceptance denominator remains 231606 with 940 copies. No stubs in that acceptance test. If valid contract requires adaptation, document it; don't reject legitimate copies to fake security.
B. Integrated access uses hardened scoped importer/no sys.modules publication across ALL loading paths, including denominator guard/anchor reads. Fresh revalidation on every actual consumption, no retained trusted snapshot.
C. Run adversarial new suite against integrated API, both normal and deliberately broken variants; test names/shapes aren't evidence. Mutation controls must actually fail. Preserve original expectations; add new candidate expectations explicitly. Audit run_matrix.py: it currently only checks expected-fail presence, not full test count and old-suite verdicts. Harden collector fail-closed for skipped/missing/unparsed tests, ERROR, crashed child, unexpected old-suite failure, not just expected FAIL.
D. Threat-model decision: ambient hashlib.sha256 monkeypatch test is reproducible but requires in-process mutation, not merely a file. Existing gate disclaims arbitrary same-process code attacker. Capture trustworthy digest primitive at trusted import if feasible through full chain; otherwise explicitly distinguish an out-of-model hardening test from an in-model blocker, do not silently drop test or claim a secure sandbox. NO claiming full acceptance while silently leaving a failed agreed test; expose any scope exception as BLOCKED/CONDITIONAL for coordinator review.
E. Run original V7/V8/V9 tests unchanged, both V10 component suites, real-entrypoint E2E and mutation matrix. Test receipts must include raw stdout/stderr, exit codes, exact target SHAs, counts, skips, expected negative controls. Write receipts outside historical frozen receipts. Repeat concurrency acceptance a bounded number (e.g. five), no open-ended churn.
F. Label all new narrative reports exactly [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. LongMemEval-only, repair candidate, no storage measurements. Refresh new layer manifest correctly excluding its own hash or via detached digest.
G. Preserve work continuously on persistent disk. Commit changes locally. Final report concise: SHA, single public entrypoint, actual tests, exact unresolved failures, no exaggerated closure. Do not say 'all pass' when negative controls intentionally fail; state them separately.
