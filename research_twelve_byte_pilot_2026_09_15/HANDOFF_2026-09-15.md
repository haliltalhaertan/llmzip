# llmzip — compact-code retrieval: what one measurement session established

[LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION]
[DISCLOSE-BEFORE-USE]

Date: 2026-09-15 · Benchmarks: LongMemEval (LME), PerLTQA, RealTalk ·
`main` untouched, nothing pushed, no frozen byte edited.

You are taking over a compact-memory retrieval programme. Read this, then
**verify from the artifacts, not from this document.** Every number below has
a file behind it, listed in §8.

---

## 1. What the system is

Per question, from that question's own archive text:

```
Z = hstack([ LSA32(word-TFIDF, seed 5101), word-TFIDF, char-TFIDF ])     ~10^5 columns
Y = normalize( TruncatedSVD(96, random_state=5204).fit_transform(Z) )
C = Y - mean(Y)
SIGN96 = (C >= 0)                                              96 bits = 12 B
```

There is **no neural embedding and no pretrained model anywhere**; the
protocol forbids them for the frozen tasks. The fit is **per question** by
design, not a defect. One indexed unit = one conversation turn,
`[date] role: content`; gold is a turn flagged `has_answer`.

Archives: LME 470 archives / 231,606 docs; PerLTQA 30 / 12,288;
RealTalk 10 / 8,944.

## 2. The metric that was missing

The programme measured expected fractional R@3. The session added **hit@k** —
"did at least one gold document land in the top k" — under the same
uniform-random-within-tie convention the frozen `exact_frac` uses. A `22.9 %`
figure circulating as "SIGN96 hit@10" is **not that**: it is standardized
float's FR@3 on RealTalk. The real starting point:

| arm | bytes/doc | PerLTQA | LME | RealTalk |
|---|---|---|---|---|
| SIGN96 (`sym`, Hamming) | 12 | 75.76 | **86.08** | 46.68 |
| asymmetric (`asym`) | 12 | 80.19 | 85.74 | 43.55 |
| B8 | 12 | 80.12 | 85.11 | 41.56 |
| sign88 (drop-only) | 11 | 79.56 | 85.32 | 42.27 |
| float96 (uncentered ref) | 768 | 80.81 | 82.55 | 36.60 |
| float_std (standardized) | 768 | **83.96** | **88.30** | **48.51** |

## 3. Five doors, measured shut

**a. More bytes.** 12 B -> 768 B buys +2.2 pp on LME and +1.8 pp (ns) on
RealTalk. Only PerLTQA shows a real gain (+8.2 pp).

**b. Better bit allocation.** At hit@10, `b8 - sign88` is statistically zero
on all three (PerLTQA +0.56 [-0.01, 1.15]). `asym - sym` is +4.43 pp on
PerLTQA but -0.33 and -3.13 elsewhere: benchmark-divergent, never pool.

**c. Different retrieval unit.** Sentence / 2-sentence / turn / turn+prev /
session, compared at equal **characters of text shown** (not "top-10 units",
which is meaningless across unit sizes). At a 4,000-character budget:
turn 70.8 %, sentence 68.8 % — and sentence costs **82,624 B of storage vs
5,947 B**, i.e. 14x more for nothing. On the strict measure (whole gold turn
covered) turn wins 70.8 vs 12.5. **The production unit is already optimal.**

**d. More dimensions.** Z (no projection, ~10^5 columns) vs SVD96, 180 LME
questions, paired: `Z - svd96 = +3.89 pp [1.06, 6.72]` for the float arm, and
`Z - svd384 = -0.56 pp [-1.65, 0.53]` — i.e. **384 dimensions already capture
everything the full feature space has.** But the production arm is the *sign*
arm, and SIGN96 at 12 bytes scores 86.08 against Z's 86.1: for the deployed
scorer there is no projection bottleneck at hit@10 at all.

**e. Compressing the 12 bytes.** First-order entropy is **95.7 of 96 bits** —
the pipeline's orthogonalization plus mean-centering has already whitened
them. Best lossless gain: LME 11 %, PerLTQA 5 %, RealTalk **negative**.
Per-code compression inflates by 2.7x (gzip header on 12 bytes).
Similarity-ordering loses: the permutation costs more than it saves.

## 4. What did work

**Deleting bits, not compressing them.** Random noise is both incompressible
*and* useless; the two facts are consistent. With the frozen variance
ordering:

| bits | bytes | LME hit@10 | delta vs 96 |
|---|---|---|---|
| 96 | 12 | 86.08 | — |
| 88 | 11 | 85.80 | -0.28 ns |
| **80** | **10** | **84.97** | **-1.11 ns** |
| 72 | 9 | 84.27 | -1.81 * |
| 48 | 6 | 79.17 | -6.91 * |

**10 bytes is free on LongMemEval.** Not on the others (PerLTQA -0.63 pp at
11 B, RealTalk -1.69 pp). If all three must hold, 12 bytes is correct.

Degradation is smooth, so this is a **quality/space dial with a measured
price**, and below ~48 bits the `dot` scorer overtakes `hamming` (LME 80.21
vs 79.17; RealTalk 39.01 vs 37.54 at 64 bits) at **zero extra stored bytes**.

## 5. Where the cost actually is

```
BUILD  per archive (TF-IDF 1.04 s + SVD96 3.81 s)      4.85  s
SCORE  one query against a whole archive             0.008  ms
```

**Build is ~600,000x the score.** Every byte-level result above lives in
0.0002 % of the compute, and build is identical for every arm.

Scoring, real archive sizes (~500 docs), single-threaded:

| arm | stored width | index RAM | ms/query |
|---|---|---|---|
| float96 | 768 B/doc | 0.399 MB | 0.008 |
| sign_naive (today's `lib_b8`) | 12 B code, float compare | 0.395 MB | 0.071 |
| sign_u32 (12 B, 3 x uint32) | **12 B/doc** | **0.006 MB** | 0.015 |
| sign80_u32 (10 B stored) | **10 B/doc** | 0.005 MB | 0.015 |

**RAM 64.7x smaller at every scale** (77.6x at 10 B). The CPU picture went
through two corrections and is stated here in its final form:

- The first measurement used a 12 x uint8 layout and reported packed as ~3x
  SLOWER than float. **96 bits is exactly 3 x uint32**, so the same row can be
  viewed as 3 elements with *no padding*; that alone takes the ratio from
  0.30x to 0.62x on LME. A uint64 view is marginally faster still but needs
  16 B/doc — **+33 % RAM**, which defeats the point, so it is rejected.
- `lib_b8`'s Hamming arm compares against the *float* matrix, so in production
  today the byte budget buys neither the RAM nor the speed.
- With a compiled binary index the loss reverses entirely — see §11.

Totalled over **every archive of all three benchmarks** (`COSTFULL.json`,
packed popcount verified equal to the frozen scorer on all of them):

| | PerLTQA | LME | RealTalk |
|---|---|---|---|
| archives / queries | 30 / 8,265 | 470 / 470 | 10 / 705 |
| float96 index | 9.54 MB | 179.73 MB | 6.94 MB |
| sign_packed index (12 B) | 0.15 MB | 2.78 MB | 0.11 MB |
| sign80 index (10 B) | 0.12 MB | 2.32 MB | 0.09 MB |
| RAM ratio | 64.7x | 64.7x | 64.7x |
| CPU ratio, uint8 layout (superseded) | 0.34x | 0.30x | 0.31x |
| CPU ratio, uint32 layout (12 B, no padding) | **0.55x** | **0.62x** | **0.71x** |
| build total | 2.4 min | 38.0 min | 0.8 min |
| score total | 0.250 s | 0.014 s | 0.030 s |

Three things this settles:

- The 64.7x RAM win is **identical across corpora** — it is arithmetic
  (768/12), a property of the representation, not an empirical finding, and
  should not be reported alongside measured timings as though it were one.
  The CPU ratio is *not* corpus-identical once measured properly (0.55–0.71).
- **WITHDRAWN: "10 bytes is faster as well as smaller."** That was an artifact
  of the uint8 layout, where 10 B was 10 elements and 12 B was 12. Under the
  uint32 layout both 80 and 96 bits are 3 words and score at the same speed.
  **10 bytes is a RAM win only** (77.6x vs 64.7x), and §4 already prices its
  quality cost. The deletion win pays on two axes, not three.
- Build amortizes over queries per archive, and the corpora differ wildly:
  LME has 1 query per archive (worst case, build/score = 163,000x), PerLTQA
  has 275 (best case, 576x). Scoring would only equal build at ~161,000
  queries per archive, so **build dominates everywhere** — but by a margin
  that spans three orders of magnitude, and any latency claim must say which
  regime it is in.

## 6. Two findings that outrank the rest

**a. The +10 pp headline is against an unstandardized float.**
LME: `sym 54.21 - float 44.16 = +10.05` reproduces the frozen `G = +10.038`.
Standardize the same float and it becomes `55.74`, i.e. **SIGN96 is 1.53 pp
behind**. `float_std` is best or tied-best on all three benchmarks at hit@10.
It costs 768 B/doc, so it is a ceiling, not a 12-byte competitor — but the
sentence "sign quantization beats float" only holds against a weak reference.

**b. RETRACTED — "the sign code is numerically fragile" was my own bug.**

The original claim rested on rebuilding from raw text and seeing `sym` drift
86.08 -> 85.55 while `float` reproduced exactly. **The drift was a wrong
seed.** The producer uses TWO seeds and this document previously conflated
them:

```
LATENT_SEED = 5101   ->  the LSA32 stage
MIXED_SEED  = 5204   ->  the FINAL SVD96          <- the representation
```

(`v52_t4f1_beam_retrieval.py:51-54`; `v52_t4c2_centering_geometry.py:37` is
5204.) Every rebuild script here used 5101 for the final SVD96. An external
reviewer caught it; repo bytes confirm it; `audit_seed.py` settles it by
rebuilding 12 LongMemEval archives from raw text under both seeds and
comparing sign bits to the frozen cache:

```
seed 5101 :   0/12 archives bit-exact   (21.71 % of bits differ)
seed 5204 :  12/12 archives BIT-EXACT   ( 0.00 % of bits differ)
```

**The pipeline is exactly reproducible.** There is no fragility band, and the
consequence drawn from it — that R8 +0.11 pp, sign88 -0.28 pp and
`b8 - sign88` +0.90 pp sit inside a noise band — **is withdrawn**. Those
effects must be judged on their own confidence intervals.

**What the same measurement does show, in the opposite direction:** two seeds
of the same randomized SVD disagree on **21.7 % of all bits**, yet hit@10
moved only 0.53 pp. A fifth of the code can flip for half a point. That is
evidence the representation is **robust** to SVD basis variation, not fragile
— the inverse of what was published here. It has not been measured on the
frozen task's own metric, so it is an observation, not a verdict.

**Artifacts built with the wrong seed, and therefore NOT the production
representation:** `run_bottleneck.py`, `run_chunksweep.py`,
`run_svdopt.py::method_A`, `run_svdvalidate.py`. Their results are flagged in
place and need re-running under 5204 before any of them is cited. Everything
that reads the frozen caches instead of rebuilding — `run_hit10.py`,
`run_ksweep.py`, `run_dropsweep.py`, `run_codesize.py`, `run_costfull*.py`,
`run_faster.py` and all faiss scripts — is unaffected.

## 7. An optimization that is ready, and its catch

Z is ~500 x ~100,000: wider than it is tall, so its rank is at most N and the
whole factorization fits in an N-dimensional space. Instead of randomized SVD
on the wide side plus a 96 x 100,000 projector:

```
G = Z Zᵀ  (500x500)   ->  eigh  ->  U, S
fit_transform(Z) = U S
Zq V = (Zq Zᵀ) U S⁻¹        <- the 74.8 MB projector is never built
```

Measured: **SVD 19.2x faster, projector 74.8 MB -> 0 B**, total build 3.6x.

Catch: it is exact, and the production path is *approximate*, so they are not
the same object.

**The measured quality comparison is INVALID and must be re-run.** It scored
the exact path against `method_A`, which used seed 5101 — not the production
representation (§6b). The numbers it produced (+0.21 pp float, -1.67 pp sign
on 470 LME questions) compare *exact* against *a non-production randomized
draw*, which is not the question. An external reviewer measuring the build
side independently found 4.22x (LME panel) and 3.57x (PerLTQA panel) speedups,
consistent with the 3.6x here, but also reported that standardized-float
quality moved on their small panel — so "free for float" is not established
either. Nothing about adopting this may be decided until it is re-measured
with seed 5204 on both arms.

## 8. Artifacts

All under `llmzip-work/parallel_ideas_r1/hit10/`, each script self-checking
against the frozen `lib_b8` (`frac@3` must equal `exact_frac`; `hamming@96`
must equal production `sym`; packed popcount must equal the frozen scorer):

| file | what |
|---|---|
| `run_hit10.py` / `HIT10.json` | hit@k and frac@k, six arms, three benchmarks |
| `run_ksweep.py` / `KSWEEP.json` | full hit@k curve; candidates needed for 90/95/99 % |
| `run_bottleneck.py` / `BOTTLENECK_shard*.json` | Z vs svd384/192/96, 180 LME questions |
| `run_chunksweep.py` / `CHUNKSWEEP_shard*.json` | five retrieval units at equal character budget |
| `run_codesize.py` / `CODESIZE.json` | lossless compressibility of the payload |
| `run_dropsweep.py` / `DROPSWEEP.json` | 96..16 bits, two scorers, three benchmarks |
| `run_cost.py` / `COST.json` | RAM and CPU per arm, real + synthetic scale |
| `run_costfull.py` / `COSTFULL.json` | the same totalled over every archive |
| `run_svdopt.py` / `SVDOPT.json` | exact Gram SVD, speed and memory |
| `run_svdvalidate.py` / `SVDVALIDATE_shard*.json` | its quality on all 470 LME questions |
| `run_costfull2.py` / `COSTFULL2.json` | uint32 vs uint64-padded vs uint8 layout, every archive |
| `run_faster.py` / `FASTER.json` | numpy call-count and batching levers; uint16 LUT rejected |
| `run_faiss.py` / `FAISS.json` | first compiled-index pass — **flawed protocol, superseded** |
| `run_faiss_deep.py` / `FAISS_DEEP.json` | 90 archives / 9,020 queries: correctness, ties, speed |
| `run_faiss_float.py` / `FAISS_FLOAT.json` | binary vs float32 cost at equal output shape |
| `audit_*.py` / `AUDIT_*.json` | **independent adversarial audit** of the above — see §11 |

## 9. Standing rules you inherit

- `main` is untouched; findings go to a side branch; the frozen tasks
  4C1–4D and the Task 4F1 / BEAM boundary are not to be run or read without
  explicit authorization from the Head Researcher (the user).
- **Benchmarks are never pooled.** Cross-benchmark divergence is a finding.
- Label every output with the four tags at the top of this file.
- Report **totals**, never bytes-per-vector — that is the quantity that
  improves when the vector count is inflated, and three rounds of guard code
  exist because of it.
- Implementer is not auditor.

## 10. Open, in priority order

1. **Record the standardized-float correction (§6a)** — a ledger entry, not an
   experiment. Every new round built on the old headline carries the error.
2. **Quantify the sign-code fragility band (§6b)** on the frozen task's own
   metric, then re-check every reported effect smaller than it.
3. Adopt the Gram SVD for float-side work (free); decide separately on the
   sign arm.
3b. **Write the exporter for the `faissdeep/` bridge and widen it** (§11). The
   90-archive bridge has no generating script in the tree and covers only
   50 of 470 LME archives, which is why LME is the one benchmark whose
   tie-breaking delta disagrees in sign.
4. Only then: the feature space itself is the remaining lever. TF-IDF cannot
   match "how long did I wait" to "it took over a year". That means a
   different representation family, which the frozen protocol excludes — so a
   separately labelled branch.
   **UNVERIFIED CITATION:** an earlier draft named "MHR, 2026, 32 bytes,
   Recall@100 = 0.65" as the bar to beat. That figure was carried over from
   another document and has NOT been confirmed against a primary source, by
   this session or by an external reviewer who looked for it. Do not use it
   as a target. Separately: recall figures from different corpora are not
   comparable, so even a verified number would not be a threshold for ours.

**Do not** reopen: more bytes, bit reallocation, chunk size, more SVD
dimensions, or compressing the payload. Each is measured shut above, with the
file that shows it.

---

## 11. The compiled binary index, and what an independent audit did to it

A compiled binary index (`faiss.IndexBinaryFlat`, 96 bits) was tested in an
**isolated** environment (`venv_faiss`, faiss 1.15.0 / numpy 2.5.3). The
frozen venv was not modified and still has no faiss; the only bridge between
the two is exported `.npz` files. This matters because §6b established that
the sign code is numerically fragile, so a dependency change is a scientific
risk, not merely an operational one.

Everything in this section was then handed to an **independent adversarial
auditor** whose brief was to break the claims, not confirm them. The auditor
re-derived every number with its own `audit_*.py` code and did not import the
`run_*.py` files. Its corrections are folded in below and flagged.

**Correctness — survives, on stronger evidence than was first offered.**
The original check compared *sorted top-10 distance multisets*, which cannot
detect the index returning the wrong documents with the right distances, and
the "full vector" check covered query 0 only. The audit ran the full version:
**0 mismatches in 4,056,452 distance cells** across 90 archives / 9,020
queries, 0 label/distance inconsistencies, and — independently — re-derived
the packed codes from the float archives to confirm the bridge really is the
frozen `sym` arm (0/90 byte mismatches).

**But the index is valid for production and INVALID for measurement.**
96-bit Hamming produces heavy ties. The frozen metric breaks them uniformly
at random; faiss breaks them deterministically, by ascending internal index.
A tie straddles the k=10 boundary in **70.3 %** of queries, and the two
conventions disagree on 311 of them:

```
hit@10, frozen convention (expected value)  73.54 %
hit@10, faiss determinism                   73.95 %
delta  +0.407 pp, cluster-bootstrap CI [0.210, 0.607]
```

*Two cautions on that number.* It is a query-weighted pool across three
benchmarks and is **92 % PerLTQA by weight** — the programme forbids pooling,
and this figure should never have been computed. Per benchmark: PerLTQA
+0.440 [0.230, 0.630]; RealTalk +0.128 [-0.54, 0.81] ns; LME **-1.00**
[-3.0, 0.0]. And "311 queries flip" is the wrong phrase: 311 is the count of
queries where the two *conventions* differ, not a realized count.

**Why it drifts upward: document storage order carries signal.** This was a
hypothesis until the auditor ran the test that settles it — permute document
indices within each archive (identical codes, identical distances, identical
gold, only storage order changed), rebuild, re-measure, 60 times:

```
real storage order     +0.4073 pp
permuted order         -0.021 +- 0.074 pp     (p < 1/60, z = 5.78)
```

Ascending-index tie-breaking systematically favours gold. Two caveats: the
effect is invisible unless you condition on the 311 decisive queries (across
all 790 gold-bearing straddling buckets the mean gold rank is 0.4993, i.e.
dead uniform), and it is **document storage order** — that this order *is*
conversation order remains an unverified label on a real statistical effect.

**Speed — one figure survived, one was retracted.**

| comparison | value | note |
|---|---|---|
| faiss binary vs best numpy | **3.4x** | audit reproduced it and could not beat the numpy baseline (4 kernels tried, best 1.5 % better); at equal output shape it is 3.8x |
| faiss binary vs faiss float32 | **~1.51x** | **RETRACTED: the published 1.31x was wrong.** |

The retraction is a self-inflicted wound with a named cause: `reps` was set to
`min(nq, 60)`, and 50 of the 90 archives are LME with `nq = 1`, so half the
corpus was timed with a **single call**. That produced *bias, not variance*
(repeating one query keeps the cache warm, so the quartiles never warned), and
it depressed the LME figure by ~32 %.

**The deeper problem with both speed numbers.** The audit swept N and found a
fixed per-call Python/SWIG overhead of **~4.6 us**, which at the median
archive size (N ~ 484) is **73–78 % of the entire measured time**:

| N | float32 / binary | numpy / binary |
|---|---|---|
| 484 (our archives) | 1.53x | 3.49x |
| 4,096 | 3.10x | 4.52x |
| 65,536 | 5.86x | 5.03x |
| 262,144 | **8.20x** | 6.36x |

So both headline ratios are largely measurements of *call overhead*, and they
**understate** the 12-byte code's kernel advantage by more than 5x at realistic
corpus sizes. Neither number may be quoted without "at N ~ 500, one query per
call."

**Net position.** The CPU deficit of §5 is closed — the 12-byte code is
faster *and* 32x smaller than float32 at equal output shape, more so as the
corpus grows. But a compiled index must not be used to produce a reported
retrieval number, because its tie-breaking is not the frozen convention and
the difference is measurable.

**Also open from the audit, not yet done:** the two faiss scripts are
inconsistent (one normalizes before `IndexFlatIP`, one does not, so the float
index is not the frozen float arm — cost-neutral but untidy); and
`run_faiss_float.py` disclaims a quality comparison the programme already has,
since `HIT10.json` shows the 96-bit code beating raw float cosine on two of
three benchmarks.

---

## 12. Two more benchmarks, and the PerLTQA outlier pinned down

The three-benchmark table became **six conditions over four dataset
families**: LongMemEval, PerLTQA (three releases), RealTalk, LoCoMo. All six
run through `run_hit10.score_archive`, which asserts
`frac@3 == lib_b8.exact_frac` for every arm on every query.

hit@10:

| arm | LME | PerLTQA en_v2 | PerLTQA en_v1 | PerLTQA zh | RealTalk | LoCoMo |
|---|---|---|---|---|---|---|
| sym (12 B) | 86.08 | 75.76 | 77.62 | 71.43 | 46.68 | 43.84 |
| asym | 85.74 | 80.19 | 87.03 | 73.81 | 43.55 | 38.57 |
| b8 | 85.11 | 80.12 | 88.29 | 75.26 | 41.56 | 36.48 |
| sign88 | 85.32 | 79.56 | 86.72 | 74.23 | 42.27 | 37.33 |
| float (768 B) | 82.55 | 80.81 | **91.16** | **82.22** | 36.60 | 34.66 |
| float_std (768 B) | **88.30** | **83.96** | 89.48 | 77.90 | **48.51** | **50.03** |

**Caveat on reading across PerLTQA releases:** en_v2 is 30 archives / 8,265
queries; en_v1 and zh are 31 / 8,548 (one archive excluded in each, N=31,
cannot support SVD96 -- the frozen port rule is to exclude, never silently
shrink). These are NOT matched cohorts, so version-to-version differences are
not a clean language or release effect.

**a. The standardized float is not universally the strongest reference.**
Section 6a said float_std is best or tied-best everywhere. That holds on four
conditions but **fails on both PerLTQA en_v1 (+1.67) and zh (+4.32), where
raw float wins.** It is a PerLTQA property, not a language one.

**b. LoCoMo's gap against its own historical anchor is the GOLD, not the
metric.** `frac@3` for `sym` is 23.7907 here against the historical gate's
23.6547 on what looks like an identical query set (1,540 / 1,535 valid / 5
excluded / 10 archives). This was first flagged as unexplained, with the guess
that the historical anchor used a different metric implementation. **That
guess was wrong.** `audit_locomo_anchor.py` ports the historical `exact_exp`
(`theory_benchmark_test_v1/locomo/run_locomo.py:75-90`) verbatim and runs it
against `lib_b8.exact_frac` over the same scores and the same gold:

```
historical exact_exp   23.790671959
frozen     exact_frac  23.790671959
max per-query difference   0.000e+00   (1535 queries)
```

They are the same estimator -- expected fraction of gold retrieved, by
linearity of expectation. The cause is `run_locomo.py:189`, `ag =
Q[qid]["ag"]`, i.e. `correct_evidence`: **the historical anchor uses the
AUDITED gold with the 156 corrections, and our caches carry `raw_evidence`
only.** Reconciling those is the open Head-Researcher obligation that blocks
Task 4F1, so the 0.136 pp gap is a direct readout of how much that unresolved
decision is worth on this metric. Separately, no historical LoCoMo float
anchor ever existed
(`theory_benchmark_test_v1/audit_locomo_provenance/REPORT.md`), so the float
column here is a fresh measurement, not a replication.

**c. The PerLTQA outlier is real and its cause is the SCORER, not the bits.**
PerLTQA is the only family where `b8`/`asym` beat `sym`. An external reviewer
noted that ~85 % of `b8 - sym` is already present in `sign88 - sym` and
hypothesised that *dropping coordinates* is what helps. The arithmetic is
right; the hypothesis is **refuted by `DROPSWEEP.json`**, where the hamming
curve on PerLTQA falls monotonically with every bit removed:

```
bits    96     88     80     72     64     56     48
      75.76  74.62  74.33  73.71  72.64  71.42  70.24
```

The actual mechanism: `sign88` is not a Hamming arm. `lib_b8.py:192-195`
scores `cosine(float q, sign-reconstruction)` -- **the query stays float.**
The identity is exact: `dot@88 = 79.56 = sign88` and `dot@96 = 80.19 = asym`.
So

```
sign88 - sym  =  +3.80
              =  keeping the query in float   +4.43
              +  dropping 8 coordinates       -0.63
```

Coordinate dropping *hurts*; it is merely outweighed. The whole effect
collapses back into the already-recorded `asym - sym` finding.

**The open question this sharpens:** why does binarising the QUERY cost
4.43 pp on PerLTQA when it is free or beneficial elsewhere
(`asym - sym = -3.13` on RealTalk)? PerLTQA archives are small (pool ~235 vs
413-883) and built from structured fields rather than free conversation, but
neither has been tested as the cause.

## 13. The projector identity: same representation, cheaper

An external reviewer proposed something sharper than section 7's exact-Gram
path. If the columns of `V` lie in the row space of `Z`, then `V = Z^T A` and

```
z_q V = (z_q Z^T) A
```

-- the SAME transform, without ever storing the wide projector. Unlike
section 7 this selects no new factorization, so it should not move the
representation at all. `audit_projector.py` checks it rather than arguing it.
Six LongMemEval archives, production seed 5204:

| quantity | result |
|---|---|
| relative residual of `V` after projecting onto row space of `Z` | 4.7e-12 |
| max query-projection difference | 9.2e-13 |
| query SIGN bits differing | **0 / 6 archives** |
| projector `V` -> factor `A` | 451 MB -> 2.2 MB |
| query projection time | 26.0 ms -> 7.5 ms (**3.47x**) |

**Four limits that must travel with this result:**

1. **Zero SIGN-bit difference is not bit-identical floats.** The projections
   differ at ~1e-12. The claim is "numerically equivalent projections and
   zero sign-bit change on the six archives and queries tested", not a
   guarantee for every archive, query and machine. Coordinates near zero
   *after* normalisation and centering are the thing to keep checking.
2. **2.2 MB is the factor `A` only, not the encoder.** This path needs `Z`
   too: **46.5 MB sparse across the same six archives.** Vocabularies and the
   other fitted models are on top of that.
3. **3.47x is the projection step alone**, measured here. It cannot be applied
   to the ~14.93 ms end-to-end query-encoding figure measured elsewhere on
   different hardware and a different panel; that figure covers a longer
   chain. Both must be re-measured in one run.
4. **The gain is not SIGN96's.** Any arm over the same representation --
   including the float reference -- benefits equally. A later comparison must
   not race an optimized SIGN96 against an unoptimized float encoder.

Also still open: `A` is currently obtained *after* building `V`. Not storing
the projector at query time is demonstrated; never building it during fit,
and cutting fit CPU, is a separate piece of work.

## 14. The per-question oracle, and what it does not bound

`audit_oracle.py` reproduces an external reviewer's per-question oracle
exactly (90.21 LME / 86.07 PerLTQA / 54.98 RealTalk; 51 RealTalk queries where
`sym` strictly beats `float_std`). Two framings are added here, and one claim
made in passing is withdrawn.

Added: the oracle must be read against the **best fixed arm**, which is
`float_std`, not against `sym`. Its headroom is then +2.11 (PerLTQA), +1.91
(LME), +6.47 (RealTalk) pp -- not the ~10 pp a `sym` baseline suggests. Win
counts are lopsided: PerLTQA 210 vs 961, LME 9 vs 21, RealTalk 51 vs 66.

**WITHDRAWN:** the statement that this bounds the value of combining the two
scorings. It does not. The oracle bounds only *selecting one of two finished
ranked lists*. A method that fuses SCORES, or learns a new representation, can
exceed it: if two arms both rank the gold 11th while each promotes a different
set of ten false positives, both score 0 at k=10 and so does the oracle, yet
averaging the scores can put the gold first. Whether that happens on our data
is untested; the point is only that the oracle is not a ceiling on it.

Lopsided win counts likewise do not prove a selector is unlearnable, and
balanced ones do not prove it is learnable. The defensible statement is that
RealTalk shows the largest selection headroom, not that it is the only
promising case.

## 15. Artifacts added after the first publication

| file | what |
|---|---|
| `audit_seed.py` / `AUDIT_SEED.json` | the seed defect of section 6b, settled bit-exactly |
| `run_locomo.py` / `LOCOMO.json` | LoCoMo on the six arms, with `ANCHOR_MISMATCH` |
| `run_perltqa_variants.py` / `PERLTQA_en_v1.json`, `PERLTQA_zh.json` | the two unused PerLTQA releases |
| `audit_oracle.py` / `AUDIT_ORACLE.json` | oracle, anti-oracle, fixed-arm baseline, win counts |
| `audit_projector.py` / `AUDIT_PROJECTOR.json` | the `V = Z^T A` identity and its cost |
| `audit_locomo_anchor.py` / `AUDIT_LOCOMO_ANCHOR.json` | the LoCoMo anchor gap is the gold, not the metric |

## 16. Claims withdrawn or narrowed on 2026-09-16

| claim | status |
|---|---|
| "the sign code is numerically fragile" (section 6b) | **withdrawn** -- a wrong seed of mine; 5204 rebuilds bit-exactly |
| Gram-SVD quality verdict (+0.21 float / -1.67 sign) | **invalid** -- measured against a non-production randomized draw |
| "float_std is best or tied-best on every benchmark" | **narrowed** -- raw float wins on PerLTQA en_v1 and zh |
| "standardization only hurts on Chinese" | **wrong** -- it also loses on PerLTQA en_v1; a dataset property |
| "six conditions" miscounted as seven | **corrected** -- 6 conditions, 4 dataset families |
| "the oracle bounds the value of combining two scorings" | **withdrawn** -- it bounds selection only |
| "the char channel carries the Chinese representation" | **withdrawn** -- feature counts are not channel contribution; needs an ablation |
| "PerLTQA's b8 gain comes from dropping coordinates" (external hypothesis) | **refuted** -- dropping hurts monotonically; the gain is the float query |
| "MHR 2026 / 32 B / Recall@100 = 0.65" as the bar | **unverified** -- no primary source found; also not comparable across corpora |
| "the historical LoCoMo anchor used a different metric" | **wrong** -- the metrics are bit-identical; the gap is the audited-vs-raw gold |
| "stage 1 swept uniform shifts" | **wrong** -- per-coordinate quantiles are per-coordinate thresholds |
| "shuffled beats learned, so placement is useless" | **corrected** -- with 20 shuffles the learned arm is indistinguishable from the control, not beaten by it |

---

## 17. Writing the 96 bits differently: two more doors measured shut

**a. Run-length encoding makes the code BIGGER.** The proposal was to write
`111110001100` as (5x1)(3x0)(2x1)(2x0). `run_rle.py` measured it on every
code of every benchmark:

| | LME | PerLTQA | RealTalk | LoCoMo |
|---|---|---|---|---|
| mean run length | 1.98 | 1.98 | 1.98 | 1.98 |
| RLE size | 110.0 bits | 109.9 | 109.9 | 110.0 |
| vs 96 bits | 114.6 % | 114.5 % | 114.4 % | 114.6 % |
| neighbour-bit correlation | -0.0022 | +0.0027 | -0.0005 | -0.0016 |

A mean run length of 1.98 is what fair coin flips give, and adjacent bits are
uncorrelated to +-0.003. The pipeline *creates* that: SVD orthogonalisation
makes the coordinates near-independent and mean-centering makes each bit
~50/50. We destroy the repetition while building the code, then cannot find
it again. This is the same wall as section 3e (95.7 of 96 bits are real
information), reached from a different direction. The decimal notation also
hides the accounting: a run needs a length AND a value, so four runs cost
about 20 bits where the original cost 12.

**b. A bigger alphabet loses, except on PerLTQA.** `run_alphabet.py` holds the
budget at 96 bits and spends it on `floor(96 / log2(L))` coordinates at L
levels each, quantile bins with conditional-mean reconstruction (equiprobable
levels, MSE-optimal centres — neither choice favours binary). hit@10:

| L | coords | RealTalk | PerLTQA | LME |
|---|---|---|---|---|
| 2 | 96 | **38.58** | 77.31 | **81.06** |
| 3 | 60 | 36.17 | **78.27** | 80.64 |
| 4 | 48 | 34.18 | 77.79 | 80.85 |
| 8 | 32 | 33.48 | 76.70 | 77.66 |
| 16 | 24 | 32.91 | 74.62 | 75.11 |
| 256 | 12 | 26.95 | 61.63 | 68.94 |

Many coarse questions beat few precise ones: measuring an axis more finely
adds little to what that axis already told you, while opening a new axis adds
something wholly new. This is section 3a (12 B -> 768 B buys almost nothing)
seen from the other side.

**PerLTQA is the exception yet again** — ternary beats binary there (+0.96),
and with a quantised query the optimum moves out to four levels. That is the
fourth independent place the same dataset resists coarse quantisation.

**And the query should not be quantised at all.** Same sweep, L=2, query kept
exact versus quantised: LME **+1.70**, RealTalk **+3.26**, PerLTQA **+5.43**
pp. Positive on all three. This isolates the effect better than the
production `asym` arm, which changes query precision *and* norm handling at
once and therefore looked benchmark-divergent. A query is ONE vector, never
stored in bulk, so exact queries cost no index memory.

*Caveat on this sweep:* its L=2 arm is not the production `sym`. It thresholds
at the median and reconstructs with conditional means; production thresholds
at zero and reconstructs +-1. The gap is large (LME 81.06 vs 86.08), i.e.
**thresholding at zero beats thresholding at the median by ~5 pp** — see
section 18. The sweep is internally consistent across L, so alphabet-to-
alphabet comparison holds; the L=2 row is not a production number.

## 18. Asking better questions with the same 96 bits

The proposal: keep one bit per coordinate but move each coordinate's decision
boundary away from zero, to wherever it separates more usefully. Same code
size, same XOR/popcount scorer. Thresholds per archive and per coordinate,
shared by documents and query, fitted from document coordinates only — no
query and no gold label enters the fit.

**a. Position matters enormously, and zero is already the best tried.**
`run_threshold.py`, thresholds at each coordinate's own q-th quantile. The
`zero` arm reproduces production exactly (sym 86.08 / 75.76 / 46.68 and
float_q = asym 85.74 / 80.19 / 43.55), which validates the harness:

| threshold | LME | PerLTQA | RealTalk |
|---|---|---|---|
| **zero (production)** | **86.08** | **75.76** | **46.68** |
| median (q0.5) | 84.62 | 74.78 | 45.25 |
| q0.3 | 76.90 | 68.71 | 31.40 |
| random | 57.15 | 46.71 | 22.34 |

Zero is not an arbitrary point: the pipeline already subtracts the archive
mean, so "is it >= 0?" *is* "is it above this archive's mean?", and the
retrieval geometry is cosine on those same centered coordinates.

*Correction to an earlier description:* this was called a sweep of "uniform
shifts". It is not. `np.quantile(C, q, axis=0)` gives every coordinate its own
threshold; what is held constant is the quantile LEVEL. Stage 1 was already a
per-coordinate threshold experiment.

**b. Learned per-coordinate thresholds do not help — and the reason is the
interesting part.** `run_learnthr.py` picks each threshold to maximise
`#(far pairs straddled) - lambda * #(near pairs straddled)`, exactly as
proposed, by an exact sweep over the archive's own coordinate values. Near
pairs are top-cosine neighbours in the full float space; far pairs are the
least similar random pairs. On RealTalk all three lambda values lost (best
-2.81 pp).

`run_learnthr_diag.py` then asked the question that separates two very
different failures: **did the optimiser hit the objective it was given?**

| | RealTalk | LME |
|---|---|---|
| archives where learned J > zero J | **10 / 10** | **470 / 470** |
| near-pair Hamming (lower is better) | 36.37 -> 35.16 | 29.04 -> 27.87 |
| far-pair Hamming | 49.08 -> 49.25 | 48.88 -> 49.09 |
| **near/far separation** | **12.70 -> 14.09** | **19.85 -> 21.22** |
| **hit@10** | **46.68 -> 44.54** | **86.08 -> 85.33** |
| shuffled-threshold control (20 draws) | 44.06 +- 0.79 | 85.26 +- 0.69 |
| learned vs that control | +0.48 (0.6 sd) | +0.07 (0.1 sd) |

**The optimiser worked, in 480 of 480 archives. The proxy is what failed.**
Neighbourhood structure improved exactly as designed, in the objective and in
code space, and retrieval fell anyway. Preserving document-to-document
similarity is not merely a weak stand-in for finding the evidence for a
question — on this measurement it is actively misleading, because progress on
the proxy came with regress on the goal.

**This generalises past thresholds.** Any method tuned on document-document
geometry inherits the same risk. In particular, a future small learned
encoder should be trained on QUESTION-EVIDENCE pairs with hard negatives, not
on document neighbourhoods.

*Two corrections to how this was first reported.* "Shuffled beats learned, so
placement is useless" rested on a single shuffle and a 0.57 pp gap; with 20
shuffles the learned arm sits at the 60th percentile (RealTalk) and the 45th
(LME) — **indistinguishable from the control, not beaten by it**. And moving a
threshold to another coordinate changes its size relative to that
coordinate's scale, so the control is not purely positional. Separately, the
learned arm itself varies by ~0.7 pp between runs from the pair sampling.

*What is NOT concluded:* that no threshold can beat zero. What is concluded is
that this objective, honestly optimised, does not — and that the objective is
the part that failed.

## 19. Artifacts added 2026-09-16

| file | what |
|---|---|
| `run_rle.py` / `RLE.json` | run-length encoding: 110 bits vs 96, mean run 1.98 |
| `run_alphabet.py` / `ALPHABET_*.json` | fixed 96-bit budget across alphabets L=2..256 |
| `run_threshold.py` / `THRESHOLD_*.json` | per-coordinate quantile thresholds vs zero |
| `run_learnthr.py` / `LEARNTHR_*.json` | learned thresholds, three lambdas, shuffle control |
| `run_learnthr_diag.py` / `LEARNTHR_DIAG_*.json` | did the optimiser hit its objective (yes) |

---

## 20. The scaled query, and the audit that shrank it

The proposal: keep the production 12-byte code untouched, but score it against
a query divided elementwise by the per-coordinate document std
(`lib_b8.fit_archive(C)["std"]`, already fitted for the `float_std` arm).
`qscale = sign(C) . (q / std)`.

**It works, cluster-bootstrapped, on all three benchmarks at hit@10** --
PerLTQA +4.24 [+3.37, +5.04], LME +2.43 [+0.65, +4.30], RealTalk +2.96
[+0.82, +4.95]. An adversarial audit reproduced every number.

Then it found two things that shrink what those numbers mean.

**a. The comparison was against the CONTROL, not the baseline.**
`parallel_ideas_r1/b8/PROTOCOL.md` is explicit: A0 SYM-SIGN96 is the
*symmetric control*, A1 ASYM-SIGN96 is the *primary baseline*, A2 B8 is the
*primary candidate*, and the primary contrast is `FR_B8 - FR_ASYM`. All four
arms cost the same 12 bytes. Measuring against `sym` measured against the
weakest of them. The file was in the same directory and was not read.

Against the protocol's own arms, hit@10:

| | PerLTQA | LME | RealTalk |
|---|---|---|---|
| qscale - asym | **-0.19 ns** | +2.77 SIG | +6.10 SIG |
| qscale - b8 | **-0.12 ns** | +3.40 SIG | +8.09 SIG |

**b. On FR@3, the frozen estimand, the picture inverts by benchmark.**

| | PerLTQA | LME | RealTalk |
|---|---|---|---|
| qscale - sym | +4.34 SIG | **+0.06 ns** | **-0.14 ns** |
| qscale - asym | -0.29 ns | +3.62 SIG | +2.35 SIG |
| qscale - b8 | -0.35 ns | +6.06 SIG | +4.56 SIG |

The auditor reported only the first row and concluded the gain does not
transfer to the frozen metric. That is true against `sym`; applying the
auditor's own baseline correction consistently shows significant FR@3 gains
against `asym` and `b8` on two of three. **The honest characterisation of
qscale is that it matches whichever existing 12-byte arm happens to be best
on that benchmark, without needing to know in advance which one that is** --
`sym` on LME and RealTalk, `asym` on PerLTQA. It never loses significantly,
and on FR@3 against the best-per-benchmark arm it is a wash.

**c. Cost.** rerank-50 (popcount over all N, then qscale on the top 50)
recovers 91-100 % of the hit@10 gain at ~2x sym in numpy. The audit showed
the published ratio timed strict top-M while scoring tie-expanded top-M, and
that reranking out of the resident +-1 float matrix is not what the 12-byte
claim allows; the honest figure is 2.4-3.4x. LUT kernels are 4-6x slower in
numpy, which is a statement about numpy fancy-indexing, not about LUTs --
faiss FastScan wins with exactly that technique in SIMD.

## 21. A defect in the FROZEN scorer: `lib_b8.ndcg3_expected`

Found by the audit, verified here independently by brute-force Monte Carlo
rather than by reading the code (`audit_verify_audit.py`).

`lib_b8.py:63` computes a gold's expected discount inside a tie bucket as
`disc[pos:pos+take].mean()`, i.e. dividing by `take`, the number of slots the
bucket can fill inside the top K. A gold lands uniformly in one of `B` bucket
positions, so the expectation is `disc[pos:pos+take].sum() / B`. Whenever the
bucket is larger than the slots it can fill, the frozen code over-credits by
`B / take`.

300 synthetic tie-heavy cases x 4,000 explicit permutations:

```
frozen      mean |error| 0.0840     max 0.5460
corrected   mean |error| 0.00123    max 0.00821
119 / 300 cases off by more than 0.02
```

The error is **exactly zero without ties**, so it inflates the tied (Hamming)
arms and leaves every float arm untouched:

| | sym, frozen | sym, correct | inflation |
|---|---|---|---|
| PerLTQA | 46.85 | 45.66 | **+1.19** |
| LongMemEval | 50.98 | 49.61 | **+1.37** |
| RealTalk | 20.87 | 19.99 | **+0.88** |

`ndcg3_expected` is called from `parallel_ideas_r1/b8/run.py:276` and `:629`,
so **every nDCG@3 number in the frozen Task B output carries this bias, always
in favour of the Hamming arm.** This is not a qscale issue -- the bias works
*against* qscale -- but it is a defect in frozen code and should be fixed and
versioned before any nDCG@3 number from that task is used again.

## 22. The pipeline ladder: what the sign operation actually does

`run_ladder.py` scores retrieval at every rung of the text-to-bit chain,
rebuilt from raw text on 240 LME archives at the production seeds:

```
                  hit@1   hit@3   hit@10
S1_Z (raw TF-IDF) 52.08   72.92   85.42
S2_svd (96 dims)  34.17   60.42   83.33    step -17.92  -12.50  -2.08
S3_norm           34.17   60.42   83.33    step  +0.00   +0.00  +0.00
S4_center         33.75   60.42   82.92
S5_sign_fq        38.33   64.58   85.00
S6_sign           42.29   70.62   87.29    S4->S6  +8.54 +10.21  +4.38
```

Harness check: `S2 == S3` to 5.55e-16, as predicted -- cosine is invariant to
positive row scaling, so **row normalisation cannot change the ranking by
itself**; its effect is to change what the mean is, one rung later.

Paired bootstrap at hit@10: the only significant step is `S4 -> S6`,
+4.38 [+0.62, +8.33] -- **binarising both sides HELPS.** `S1 -> S2` is only
-2.08 ns at hit@10 but **-17.92 at hit@1** (worst-case analytic bound
[-28.09, -7.74], significant under every variance assumption). hit@10 hides
the compression loss; hit@1 exposes it.

**And the mechanism is now measured** (`run_where_next.py`). `float` and
`float_std` are the same 96 float64 coordinates; the only difference is
dividing each axis by its own std. That alone is worth, on FR@3:

| | PerLTQA | LME | RealTalk | LoCoMo |
|---|---|---|---|---|
| float_std - float | +1.41 | **+11.59** | +5.66 | +9.11 |

Now place `sym` on that scale:

```
LME       float 44.16 -> sym 54.21 -> float_std 55.74    sym captures 87 %
RealTalk  float 17.25 -> sym 22.55 -> float_std 22.91    94 %
LoCoMo    float 17.14 -> sym 23.79 -> float_std 26.25    73 %
PerLTQA   float 55.17 -> sym 48.89 -> float_std 56.58    BELOW raw float
```

**Taking the sign is a per-axis normaliser, not merely a compressor.** Forcing
every coordinate to +-1 equalises each axis's contribution exactly, which is
the same job standardisation does. That single mechanism explains the whole
pattern: why `sym` beats `float` by ~10 pp, why `sym` lands close to
`float_std`, and why `qscale` adds so little on top -- the normalisation was
already done. It also re-frames L-097 correctly: the +10 pp headline is real
and its mechanism is normalisation, not compression, which is exactly why the
advantage disappears once the float reference is normalised too.

PerLTQA is the exception on this axis as on every other: there `sym` is below
even raw float, so normalisation is not the bottleneck and the cause is
unknown.

## 23. Where the remaining value is, and what it is not

`run_where_next.py` also measured the ceiling for any second stage: if an
oracle reordered the ten candidates the 12-byte code already retrieves, what
FR@3 would follow?

| | current | oracle from top-10 | headroom | oracle from top-100 |
|---|---|---|---|---|
| PerLTQA | 53.24 | 65.24 | **+12.00** | 76.52 |
| LME | 54.27 | 78.32 | **+24.05** | 92.62 |
| RealTalk | 22.41 | 36.95 | **+14.54** | 61.30 |
| LoCoMo | 25.13 | 40.55 | **+15.42** | 72.83 |

Against +0.36 to +2.46 pp for any better reading of the same bits
(`sym -> float_std`). **The ranking is the bottleneck, not the
representation, by roughly a factor of ten.**

**But cheap signals do not reach it.** `run_signals.py`, 470 LME queries,
alpha cross-fitted on one half and scored on the other:

| signal | gold separation | best alpha | cross-fitted FR@3 | over base | % of ceiling |
|---|---|---|---|---|---|
| idf_overlap | +1.119 sd | 1.0 | 55.90 | +1.63 | 6.8 % |
| neg_length | +0.691 sd | 0.5 | 56.27 | **+1.99** | 8.3 % |
| num_overlap | +0.067 sd | 0.0 | 54.27 | 0.00 | 0 % |
| cap_overlap | +0.277 sd | 0.0 | 53.60 | -0.67 | - |
| row_index | -0.002 sd | - | - | - | - |

Dates and proper nouns carry nothing -- the archive is about the same people
in the same period. Position inside the top-10 carries nothing either (the
storage-order effect of section 11 is about exact ties, not about ranking).
Term overlap separates gold strongly but adds little, because the code is
itself TF-IDF-derived and already measures it.

Two footnotes worth keeping. A plain "prefer shorter documents" prior is worth
**+1.99 pp of FR@3 on LME, against qscale's +0.06** on the same metric -- a
useful sense of scale for how narrow that band is. And 92 % of the ceiling is
not reachable by any of these, which means the residual is genuine
question-evidence relevance, not lexical statistics.

## 24. The control the programme never had: BM25

Every arm measured in two days compares one way of compressing an SVD
representation against another way of compressing the SAME representation.
None asked whether the representation earns its keep against the oldest
lexical baseline there is. `run_bm25.py` asks. Textbook k1 = 1.2, b = 0.75,
fixed before any result, per-archive statistics, no tuning. LongMemEval, 470
queries, cluster bootstrap:

| arm | FR@3 | hit@10 |
|---|---|---|
| sym (12 B) | 54.21 | 86.08 |
| qscale (12 B) | 54.27 | 88.51 |
| float_std (768 B) | 55.74 | 88.30 |
| **bm25 (no vectors at all)** | **57.00** | 83.62 |
| **bm25 + sym, RRF k=60** | **59.14** | 87.87 |

**BM25 does not significantly beat the vector arms** -- `sym - bm25` is
-2.79 [-5.89, +0.29] ns and `qscale - bm25` -2.73 [-5.80, +0.36] ns -- so the
strong form of that claim is not established. But it is clearly competitive on
FR@3, and the vector arms are significantly better on hit@10 (`qscale - bm25`
+4.89 [+2.13, +7.87] SIG).

**They are complementary, and the fusion is the largest FR@3 gain measured in
this entire session:**

```
fr3  bm25+sym - sym        +4.92  [+2.60, +7.28]  SIG
fr3  bm25+sym - qscale     +4.87  [+2.36, +7.41]  SIG
fr3  bm25+sym - float_std  +3.39  [+0.77, +6.05]  SIG
fr3  bm25+sym - bm25       +2.14  [-0.03, +4.35]  ns
hit10 bm25+sym - qscale    -0.64  [-2.77, +1.49]  ns
```

Reciprocal rank fusion with k = 60 is parameter-free and was fixed before the
run. **+4.87 pp on FR@3 against qscale's +0.06 pp on the same benchmark and
metric**, i.e. the largest gain of the session came from adding the oldest
method in information retrieval next to the code, not from any of the
compression work.

Three caveats that must travel with it. BM25 needs per-archive term
statistics, which an external run measured at ~64 MB for LME's 470 archives
against 2.78 MB for the codes -- though the raw text must be stored anyway to
return results, so how cheaply those statistics can be derived is an
unmeasured engineering question. **Only LongMemEval was run**: the PerLTQA
query cache stores `char`, `section`, `gold` and `qC` but no question text, so
that benchmark needs the text re-derived from the raw dataset before this
generalises. And nothing here weakens the memory case -- the 12-byte code is
still 64x smaller and still significantly better at hit@10.

## 25. Artifacts added 2026-09-16 (second pass)

| file | what |
|---|---|
| `run_ladder.py` / `LADDER.json` | retrieval at every rung of the text-to-bit chain |
| `audit_ladder_ci.py` / `LADDER_CI.json` | paired bootstrap for the ladder steps |
| `run_qscale.py` / `QSCALE.json` | the scaled-query arm (NOTE: its own `ci95` is query-level; superseded) |
| `run_qscale_cost.py` / `QSCALE_COST.json` | CPU and RAM of the scaled query |
| `run_qscale_fast.py` / `QSCALE_FAST.json` | rerank and LUT kernels |
| `audit_qscale_ci.py` / `QSCALE_CI.json` | cluster bootstrap, correcting my own repeated error |
| `audit_verify_audit.py` / `VERIFY_AUDIT.json` | independent check of the audit, including the nDCG defect |
| `run_where_next.py` / `WHERE_NEXT.json` | the scaling lever and the second-stage ceiling |
| `run_signals.py` / `SIGNALS.json` | which cheap signals separate gold inside the top-10 |
| `run_bm25.py` / `BM25.json` | the lexical control and the fusion |
| `AUDIT_BRIEF_2026-09-16.md` | the adversarial brief this audit answered |

## 26. Claims withdrawn or narrowed on 2026-09-16 (second pass)

| claim | status |
|---|---|
| "qscale beats the production arm on all three" | **narrowed** -- `sym` is the protocol's CONTROL; against the primary baseline `asym`, PerLTQA is -0.19 ns |
| "12 bytes beats 768-byte float_std on two benchmarks" | **wrong** -- query-level bootstrap artifact; cluster-bootstrapped it MATCHES (+0.21 ns, +1.13 ns) |
| "keeping the query exact helps on all three" | **wrong for the production code** -- that came from the alphabet sweep's quantile code; with sign-at-zero and +-1, `asym` LOSES to `sym` on 3 of 4 benchmarks |
| "rerank50 costs 1.85-1.97x sym" | **understated** -- 2.4-3.4x once the scored algorithm and the code-resident path are the ones timed |
| "the loss is at the SVD step" | **incomplete** -- standardising the same 96 axes recovers most of it; the axes are mis-scaled, not uninformative |
| "the historical LoCoMo anchor used a different metric" | **wrong** -- metrics are bit-identical; the gap is audited-vs-raw gold |
| query-level bootstrap in `run_qscale.py` | **repeat of an error already corrected once today**; fixed in `QSCALE_CI.json`, the original file still carries the superseded interval |
