# DRAFT — pending ledger entry (LOCAL, NOT PUSHED)

**[LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]**

**Status: local draft only. For the Head Researcher to review, amend, and land when they choose.
Written by the local desktop session that ran the verifications it records (per the 2026-09-10
ledger-authorship rule); the independent recomputations it cites were run by separate Muse executor
sessions on this machine.**

```text
timestamp_utc: 2026-09-12T??:??:??Z   # fill at landing
actor_role: Desktop local session (Hermes orchestrator) under Head Researcher in-session instruction
             ("projeyi lokal olarak devam ettir; GitHub'a push yok; Muse Code'u alt ajan olarak kullan").
             Independent recomputation executor: separate Muse session 01a0970e-db02-... (LME+LoCoMo
             extension statistics); independent verification sessions: Muse (three packages, §below)
             and Muse twelve-byte audit (separate commissioned task).
predecessor_commit_or_tag: L-095 / <main head at landing>
scope: Complete the Task1 preseal-diagnostics gaps (strict >0 sign entropy, zero mass, D4 correlation
       statistics for LongMemEval; full diagnostic family for LoCoMo) by recovering "source-bound
       sufficient statistics" - the route named in next_single_action after L-094/L-095. No push, no
       seal, no arm decision, no Task4F1 contact, no retrieval recomputation anywhere.

method: The frozen mechanism-track representations (470 LongMemEval per-question C matrices; 10
        LoCoMo per-conversation C matrices) were regenerated by re-executing the byte-frozen
        producers themselves (T4C3 probe sha256 8dce37b1...; T4D script == its seal's
        sealed_compute_script_sha256) on the canonical published inputs (dataset d6f21ea9...,
        277,383,467 B; locomo10.json 79fa87e9...; audit layer manifest 90a4e94c... 20/20 files
        byte-verified), then CERTIFIED:

certification_of_regeneration:
  statistics_level: all 11 scalar heterogeneity fields and both 96-length vectors (variance,
    >=0 occupancy) reproduce at MAX RELATIVE DEVIATION 0.0 across all 470 questions vs the published
    V52_T4C3_native_heterogeneity.csv (148ae5b7...) under the pinned stack; an independent
    recomputation under a different BLAS shows last-bit drift on one cancellation-amplified scalar
    (cov_offdiag worst 2.27e-14, 329/470), so the gate in substance is <=1e-12. Published vectors
    are %.17g (round-trip exact), so equality of the checked functionals is exact.
  code_level (sign): regenerated packbits(C>=0) is BIT-IDENTICAL to the frozen packed 96-bit sign
    codes the retrieval pipeline consumed (V52_T4C2_BINARY_GEOMETRY.zip sha256 40026fe6...):
    470/470 questions, 231,606 documents, 0 mismatched bytes; query codes 470/470;
    gold_rows and N_archive cross-match 470/470.
  code_level (ITQ): refitting the frozen adapter's fit_itq on the regenerated C reproduces the
    stored 5-seed ITQ code variants BIT-IDENTICALLY on BOTH channels: document codes 470/470 x 5/5
    seeds and query codes (itq_query_packed) 470/470 x 5/5 seeds, 0 mismatches (2350 refits each;
    code_certification_itq.json + code_certification_itq_query.json; bitorder "big").
  locomo: per-conversation feature-count blocks reproduce 10/10 exactly vs the frozen
    V52_T4D representation transfer proof; frozen native R@3 anchor 0.23654714666441054 reproduced
    with absolute error 0.0 by two independent local executions; per-conversation variance and
    occupancy vectors now published as the first value-level outputs for future re-execution.
  in_principled_limits (red-team proven by construction): no frozen artifact distinguishes exact
    +0.0 entries from tiny positives (a constructed family passes all checks jointly with
    zero_mass 0.25 vs 0.0), and correlation quantiles are underdetermined by the checked scalars.
    Therefore D2/D3/D1_ge are directly certified; D1_gt/zero_mass/D4 are determinism-argument
    results on the certified regeneration, not byte-certified, and are labelled as such.

task1_extension_results (equal archive weight):
  longmemeval (470): D1_gt == D1_ge = 0.9970337299075775 (sd 0.0009689546162983);
    zero_mass = 0.0 in every archive (hence strict >0 equals >=0 BY MEASUREMENT of the
    regeneration); D4 off_mass mean 0.15188848165900984, median|r| mean 0.004568877386692503,
    p95|r| mean 0.01644504773681544 (all 96 coordinates active in all archives).
  locomo (10 conversations): D1 0.9981814455753918 (sd 0.0008680842710107); zero_mass = 0.0;
    D2 cv_sigma 0.48468867201587706; D4 off_mass 0.1284918632163776, median 0.003308774681940944,
    p95 0.01087587274446762.

independent_recomputation: a separate Muse executor session imported the frozen diagnostics
  functions verbatim and computed all quantities independently: LongMemEval all means exact, sds
  and per-archive D4 to the last ulp; LoCoMo equal to last ulp. Cross-check programmatic and
  recorded in the local session tree.
  second_party_locomo_value_reval (Muse 01a09725-8449-..., different stack: python 3.14.4 /
  numpy 2.5.3 / scipy 1.18.1 / sklearn 1.9.1): independent re-execution of the frozen T4D
  load_dataset/build_representation on re-verified inputs (both input hashes re-matched exactly);
  structural quantities BIT-EXACT (counts, shapes, zero_mass == 0.0 with 0 exact zeros, >=0 and >0
  occupancy vectors identical, entropies equal, D1_ge/D2_cv_sigma means exact); numeric scalars
  <=3e-15 absolute (worst relative 1.91e-14 on variance vectors). Cross-stack C_sha256 bytes
  differ 0/10 - LAPACK last-bit SVD drift, quantified and disclosed.
  itq_crossstack_sample: 10 questions x 5 seeds refit under numpy 2.5.3 -> stored ITQ doc codes
  matched 50/50 exactly (regen/lme/itq_crossstack_sample_result.txt).
  itq_query_channel: certification extended to itq_query_packed - the one NPZ channel previously
  uncovered - PASS: 470/470 questions x 5/5 seeds exact, doc codes re-derived in the same pass
  470/470, bitorder "big" (code_certification_itq_query.json).
  coldstart_review (of this package; Muse 01a09720-8229-..., xhigh): independent 12-question /
  2-conversation re-derivation before opening package outputs + full recounts; independently
  confirmed 0 exact zeros across all 22,234,176 LME entries and all 10 LoCoMo matrices; verdict
  "the package supports its substance" with five wording/coverage defects raised, ALL RESOLVED
  (bit-equal wording downgraded to last-ulp where accurate; extended RESULTS now fills all 470
  per-archive rows; stale filename fixed; scope qualifier attached to the code-level claim).
  closure_verification (of the fix round; Muse 01a09734-35d7-..., read-only): 5 PASS / 1 CAVEAT /
  0 FAIL. Verified end-to-end: NaN-safe upd (dynamic probe), adapter sha pins == real file in both
  scripts, ITQ query channel spot-refit 25/25 query + 25/25 doc exact (different stack),
  sha256sum -c of the manifest clean, receipt wording checks pass. The single CAVEAT (the
  "0 diffs" phrase vs 3 deliberate frozen-leaf updates: D1_gt_reason/D4_reason cleared where
  nulls are filled, locomo status advanced) was resolved by correcting the claim and
  self-documenting the three updates in the artifact (intentional_frozen_leaf_updates); the
  stale "7/7 bit-equal" string it found embedded in the extended RESULTS was regenerated with
  corrected wording.

verification_of_adjacent_packages (independent, local):
  L-093 preseal namespace: 49/49 + sub-manifests (23 + 14) hashes match; reproduction exit 0 with
    1-ulp environment drift only.
  L-095 itq_haar: 18/18 hashes; verify_package.py PASS; internal consistency corroborated.
  L-094 G3 delivery: 432/432 hashes; package_integrity PASS; the 54/5961 updated suite was executed
    to completion locally in a gate-satisfying environment (disclosed path caveat); README's
    54/5961 vs evidence/final 53/5959 adjudicated NOT a defect (initial-candidate vs delta evidence).
  V52_T4C3_ALL_OUTPUTS.zip: 26/26 manifest hashes match; seal c7cf7aa0... and post manifest
    7bfeae58... match their pinned values.
  Local programme verifiers on main 5ec3db6: verify_frozen_artifacts 26/26 match;
    verify_continuity_state PASS; verify_preregistration_seal PASS.

twelve_byte_audit (commissioned 2026-09-11, first executed 2026-09-12, Muse executor):
  C1 C2 C3 C4-measurement C5 C6 C8 C9 C11 C12 VERIFIED; C4-interpretation MIXED (functionally
  verified, theorem attribution UNVERIFIABLE offline); C7 VERIFIED with two corrections (abort is
  deferred to encode time; observed mechanism is a clean assertion abort, not heap corruption);
  C10 UNVERIFIABLE (no network for version sweep, graded as permitted). Spot-checks by the
  orchestrating session reproduced the C11 citations and the C7 abort behaviour exactly.
  Report: reports/muse_twelve_byte_audit_2026-09-12.md (local).

labels_that_must_travel_together:
  [LOCAL SESSION - NOT PUSHED]
  [CERTIFIED REGENERATION - NOT FROZEN-BYTE RECOVERY]
  [NOT AN INDEPENDENT AUDIT - cold-start review still required before citation as canonical]

outcome_boundary: Task4F1 SEALED / RUN BLOCKED / NO AUTHORIZATION / OUTCOME ACCESS FORBIDDEN
  unchanged. No experiment, seal, finalize, HMAC or production authorization. No real retrieval
  outcomes were computed, compared or inferred anywhere in this work; the regenerated matrices were
  consumed only for representation-level statistics and code-equality checks.

status: PASS WITH EXPLICIT SCOPE LIMITS - Task1 PARTIAL gaps closed by certified regeneration;
        extension artifacts staged locally; nothing pushed.
```
