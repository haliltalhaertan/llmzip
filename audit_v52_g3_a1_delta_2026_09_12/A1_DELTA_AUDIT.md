# Independent G-3 A1 delta audit

**Exact candidate:** `23fb505ae47d06b34a75a97955e5d1a3d8586de1`

**Predecessor candidate:** `24d3351f068f6982148c14a9e3338c29a2769449`

**Original rejection preserved:** `2a025ee3b85f25af80e1b939f85f23c6cf06af30`

**Date:** 2026-09-12

**Disposition: A1 CLOSED in the authorized synthetic scope. Exact delivery candidate
NOT ACCEPTED pending A2, a newly introduced manifest-order failure in its documented
package-integrity check.** No additional computation, certificate, schema or privacy
defect was found in the focused delta. No production/native/gold work was undertaken.

## What changed, independently verified

The candidate is a direct child of the original candidate. Exactly 12 paths change,
all inside `drafts/v52/membership_g3_remediation_2026_09_11/`: seven additions and five
modifications. `EXACT_FOCUSED_DELTA.diff` records the code/test/document changes.

The only implementation change is the six-line diff in `integration_g3.py`:

- `_validate_envelope` now creates a question-to-ordinal map from `sorted(qs)` and
  validates LongMemEval diagnostic ownership against that map (`:145`, `:160`).
- `prepare_synthetic_envelope` creates LongMemEval groups using `enumerate(sorted(qs))`
  (`:196`), agreeing with the unchanged pipeline's sorted-global convention.

The input `question_ids` order, prepared records and underlying pipeline are preserved.
The core, certificate family/all-96 controls, exact oracle, record boundary, runner,
ingestion and other Python sources are byte-identical to the predecessor. Of the 21
root/authoritative Python files, only the bridge and its integration test change.
The 54-line new test method is the only test AST addition; removing that method from
the new AST leaves exactly the original integration-test AST. No old assertion was
removed or weakened.

`provenance/`, `inherited/` and `evidence/final/` have zero Git diff. README and
TRACEABILITY explicitly distinguish the original rejection/53-method receipt from
the new implementation receipt. Their pending-independent-review language correctly
describes the state before this audit; this audit supplies the subsequent decision.

The 432 manifest entries match the candidate's 432 payload paths without duplicates,
omissions or additions. All 432 payload SHA-256 values and byte counts match raw Git
objects. The manifest itself is excluded from its own inventory and separately hashed.
The 484 checks in `IDENTITY_RESULTS.json` all pass; these verify byte identity, exact
scope, test preservation and receipt bindings. They do not claim that the candidate's
order-sensitive package-integrity gate passes: it fails as documented in A2 below.

## A1: original counterexample now passes

The prior independent `probe_lme_binding.py` was read from audit commit `2a025ee...`.
`PROBE_LINEAGE.json` and `PROBE_ADAPTATION.diff` bind the new replay to that exact
probe. Its original unsorted two-question fixture, generated bytes, actual ingestion,
representation fitting, full certificate/geometric controls, actual scoring and
forwarding score observer are preserved. The candidate pin and expected outcome change;
additional assertions check records, question order, CV and rehashed wrong identities.
No candidate implementation source is patched.

Observed against `23fb505...`:

| Ordinal | Question actually prepared/scored | Question in accepted envelope |
|---|---|---|
| 0 | `q_a` | `q_a` |
| 1 | `q_z` | `q_z` |

The original input cohort remains `['q_z', 'q_a']`; all 120 question-keyed records are
preserved. **Zero mismatches** remain. Each diagnostic's `cv_sigma_before` equals the
value returned by the actual score observer. Evidence: `A1_DELTA_PROBE.json` and
`focused_runtime/independent_a1_probe.stdout.txt`.

The audit then swaps the two diagnostic question-ID lists and independently computes
the canonical JSON SHA-256 rather than calling the bridge's hash helper. Both the
internal validator and the public `compute_synthetic_result` reject this rehashed
payload with **E-G3-I05**. Cause/context are empty; an instrumented numerical helper
is never called. Thus validation checks the ownership relationship, not merely a
stale public hash. The prior A1 failure is corrected in both builder and validator.

## New regression: executed independently

Exactly the new method
`DeliveryIntegration.test_A1_longmemeval_diagnostics_follow_actual_sorted_assembly`
was executed from the candidate's raw, unmodified test bytes. It passes **one method,
two subtests, zero failures/errors/skips**, covering both sorted and unsorted cohorts.
The forwarding observer calls actual scoring and records question ownership and CV;
the assertions compare against that observation. It also rejects swapped diagnostic
IDs after rebinding their hash. The fixture therefore exercises the composition that
the original 53-method suite lacked.

The class's unchanged setup generates its LoCoMo synthetic integration fixture and
uses the accepted bootstrap arithmetic, as in the original audit; the chosen A1
method itself exercises two generated LongMemEval archives. Source/mapping admission
is substituted only with equality checks of generated bytes and synthetic mappings.
No accepted production stamp, real source authorization or frozen anchor is created.

The exact interpreter and package lock are retained: Python 3.13.15, NumPy 2.3.5,
SciPy 1.17.0, scikit-learn 1.8.0, pandas 2.2.3, `-B`, assertions enabled,
`PYTHONHASHSEED=0`, and all seven numerical thread variables set to 1. Runtime evidence
is in `new_test_retry/NEW_A1_TEST.json` and its stdout/stderr/command receipts.

An initial attempt failed during NumPy import, before unittest ran a method: this
audit's new guard omitted the README launcher's allowance for installed
`.dist-info/direct_url.json` metadata. The guard was corrected solely for that file
under the exact locked environment's site-packages directory. The failed import
transcript is retained in `focused_runtime/new_a1_method.stderr.txt`; it is an audit
launcher error, not a candidate test failure. The successful retry ran only the new
method. The independent counterexample probe was not rerun.

The candidate's `evidence/a1_fixed` receipt reports **54 methods / 5,961 subtests,
zero failures/errors/skips**, with all 21 source hashes matching the candidate.
That full-suite result remains attributed to the implementer; this delta audit did
not rerun the full 54-method suite or any historical suite. The supplied red transcript
shows the new method failing on `['q_z'] != ['q_a']` before the fix. It was inspected
and hash-bound as implementation evidence, not relabeled an independent red replay.

## A2 — [P2, delivery-acceptance blocker] manifest order breaks the documented integrity gate

**Trigger:** run the existing README package-integrity command against the exact new
candidate. `package_integrity.py` is unchanged between the old and new candidates.

```powershell
& 'C:/Users/MDP/Documents/ChatGPT/LLM_TOKEN_ZIP/work/.venvs/g3-lock-20260912/Scripts/python.exe' -B `
  drafts/v52/membership_g3_remediation_2026_09_11/package_integrity.py `
  --ref 23fb505ae47d06b34a75a97955e5d1a3d8586de1
```

**Actual:** exit 1, `RuntimeError: file inventory mismatch` at
`package_integrity.py:56`. `paths()` returns sorted paths; `main()` compares the
declared manifest list directly with that sorted list. The new `FILE_HASHES.json`
contains all 432 correct entries in a different order, so the check fails before
its per-file hash loop. The first observed positional difference is declared
`INTEGRATION_TRANSFER.json` versus expected `A1_REMEDIATION.md`.

This is **not a payload hash mismatch, omitted file, duplicate, retrieval error or
security failure**. The independent set/duplicate/size/hash checks all pass. It is a
reproducible delivery regression in the candidate's own documented verification path.
An exploratory check and one captured repetition both failed identically. Captured
evidence: `focused_runtime/package_integrity.stderr.txt`, `COMMANDS.json`, and
`A2_MANIFEST_ORDER.json`.

**Required narrow correction:** regenerate the manifest using the existing
`package_integrity.py --write` ordering, or otherwise preserve exactly the path order
that this unmodified verifier emits. No bridge, numerical source, test or historical
evidence change is needed. Verify the corrected manifest against its exact new commit.
This audit does not make that implementation/delivery edit.

The scoped computational A1 repair is accepted. Delivery acceptance for exact
`23fb505...` remains withheld until A2 is corrected; an A2-only successor requires
only focused manifest/delta verification, provided all executed source hashes remain
identical. This is not a request for another full synthetic or historical replay.

## Remaining dispositions and preservation

- Original A1: **CLOSED** for the authorized synthetic bridge.
- Original L-081 seven findings/nine gaps and Decision 2 certificate evidence:
  retained from the original audit; their implementation bytes and evidence were
  not changed by this delta.
- G2's four dispositions and v5-successor development recommendation: retained;
  no canonical HR/ledger working-line promotion performed here.
- Obligation 4's specific A1 provenance defect: corrected. A complete deliverable
  is still held on A2. Real-source provenance remains outside this acceptance scope.
- Obligation 5: **PARTIAL/OPEN**. Independent review exists, with A2 still pending;
  real ingestion review and pre-run seal remain open and were not attempted.
- Production/native-anchor/gold/cohort/shard binding: **OPEN and unverified**.
  No new scientific policy, production source, real cohort or native/gold closure
  is inferred from the successful synthetic counterexample replay.
- Task 4F1 retains **SEALED / RUN BLOCKED / NO AUTHORIZATION / OUTCOME ACCESS FORBIDDEN**.

All audit writes are confined to the new namespace
`audit_v52_g3_a1_delta_2026_09_12/`. Candidate Python sources were materialized from
raw Git into its ignored `v52/` scratch area; `EXECUTED_SOURCE_BINDING.json` binds
them. The audit branch retains its prior base and rejection; it does not merge or
cherry-pick the candidate implementation. Old rejection artifacts are untouched.
No corpus, real gold, native-anchor outcome file, experiment, production retrieval,
seal, finalize, HMAC, production authorization, main update, Drive work, preregistration
work or cost replay was performed. Parent Task 3 integration was not duplicated.

`SHA256SUMS.json` binds the delivered delta-report/evidence artifacts and excludes
itself. This is an audit-integrity inventory, not a scientific seal. The exact Git
audit commit supplies the immutable delivery identity without a recursive self-hash.
