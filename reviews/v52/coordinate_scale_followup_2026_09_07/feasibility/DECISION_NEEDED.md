# Missing-uncertainty input feasibility and decision boundary

2026-09-07; additive continuation, not a new analysis authorization.

Remote check: main a094452, research591e5d0 unchanged; neither commissioned audit nor literature target branch returned by ls-remote. No inference about agent health.

## Feasibility established

The LoCoMo runner writes archive ordinal into each persisted tie-identity string (`locomo_coordinate_scale.py:189`). The saved metric rows consistently map 1535 question IDs to ten ordinals0..9. Sizes are150,81,152,199,178,123,149,191,156,156. The script binds the input and mapping hashes and rejects three malformed encoding controls. This recovers the producer's grouping, not an independent raw-corpus identity audit. Unequal sizes make question weighting and equal-conversation weighting different targets.

LongMemEval persists470 question IDs and global lexical tie identities. This check does not establish a shared-conversation cluster mapping. Do not relabel one question/archive as an independent conversation to produce a superficially matching bootstrap.

No bootstrap, random seed draw, ranking or new metric was computed. Existing metric coverage is validated by the earlier records package. Thus LoCoMo input availability does not require a new retrieval experiment; missing methodological decisions remain the actual blocker.

## Decision requested before implementation

Authorize only an additive, explicitly post-outcome uncertainty-completion package on frozen metric rows, retaining BOTH named aggregation routes and a fixed existing rotation panel. Before it runs, review a fully specified plan for paired question resampling and LoCoMo conversation-cluster resampling, weighting, generator/count/seed, intervals and undefined-ratio accounting. None of those unspecified values is adopted here. The original preregistration and result files stay immutable; any resulting estimates cannot retroactively be called fully prespecified.

This permission would NOT authorize new retrieval, raw corpus acquisition, new rotation trials, a new scale rule, threshold changes, main-state writes, BEAM/Task4F1 or scientific acceptance. The independent auditor must still adjudicate the aggregation/interpretation discrepancy.

Until that decision or the commissioned report arrives, the safe next step is review of this plan—not another preparatory document or a repeated numerical run. Current narrow score observations are preserved; a fully compliant mechanism verdict remains withheld.
