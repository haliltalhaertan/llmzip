# Coordinate-scale review follow-up — 2026-09-07

Status: **DRAFT RESOLUTION / NOT SCIENTIFIC ACCEPTANCE**. This additive package supplements review4769c2a; it does not replace it or amend any frozen experiment file.

## Scope and authority

The user requested continued work and allowed subagents. Two contextual subagents used separate worktrees and branches for persisted-record validation and a resolution proposal. They were not cold-start auditors. The parent worked on retrospective source custody. No new retrieval, raw-corpus access, extra seed panel, bootstrap or experimental intervention was run. Task4F1 run=0, finalize=0, HMAC=0; BEAM outcome access=false.

The remote readback in this turn confirmed main a0944522d122cfc3edb36b1d54bca3a1ade6661f (L-056) and research591e5d0fd7af8c265ac12a6176761475a19b2f02. Neither commissioned report target branch existed in that readback. This is not proof of the agents' health. The canonical state and preregistration verifiers both printed PASS at a094452; neither verdict certifies this mechanism experiment.

## What was narrowed

`custody/check_pinned_sources.py` uses Git blob bytes, never imports a research module. All ten explicit seal entries match at installation59ae1b53, trigger680b10b8 and result591e5d0: **30/30 PASS**. The trigger binds the correct seal blob. Three additional source files (LoCoMo common module and the two LongMemEval adapters) are byte-identical at all three commits. A one-byte mutation negative control is rejected.

Consequently the earlier LoCoMo closure finding remains an **enforcement coverage defect**, not observed post-seal mutation. These retrospective checks do not certify external dataset downloads, third-party environment closure or single execution in GitHub Actions.

See `records/` for actual persisted-row validation and adversarial controls; see `resolution/` for the non-authoritative additive correction and questions for the commissioned audit. Metric-record checks do not reproduce raw gold joins, Hamming distances or representation invariance.

**Actual records:** both panels PASS the added checks: 92100/28200 rows, 1535/470 observed question sets, complete ten-seed/six-arm grids, finite unit scores, exact paired native/scaled-native metric equality, repeated native scalars and matching frozen native aggregates. Eighteen in-memory malformed-record controls are rejected. The parent reran the committed validator and reproduced `records/RESULT.json` byte-identically. This narrows R4 for the existing records but does not repair or certify the old aggregator. Observed question IDs are counted, not independently joined to a frozen cohort ID manifest.

## Decision boundary and next action

The unresolved scientific issues remain registered aggregation, omitted required uncertainty reporting, and inconsistent interpretation clauses. Favorable byte checks do not settle them. The proposal preserves both aggregation routes and frozen results rather than choosing a new favorable endpoint after observing outcomes.

Next action: independent adjudication of the proposed interpretation/reporting correction and completion plan. Do not install a new runner, retrigger workflows, change old seals or alter main/state/ledger from this branch. No Drive backup was made; none is claimed. This is a completed review work package, not closure of the experiment's independent audit.

## Reproduction

Use Python with `-B` for the scripts in `custody/` and `records/`. Both are standard-library-only. The parent used the bundled Python runtime. Outputs and scripts are indexed by `FOLLOWUP_HASHES.json` (self excluded). Original review files remain unchanged.
