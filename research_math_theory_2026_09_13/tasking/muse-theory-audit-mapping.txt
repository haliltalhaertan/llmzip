Audit WHERE OUR MATHEMATICAL INFERENCE FAILED; do not produce a flattering new theory or tune a proxy to rescue prior results. Work ONLY in current /home/mdp/muse-work/theory-audit-* workspace. All prior sources READ ONLY. No Git push/main changes, installs/network, paid APIs, embeddings, new training/routing, frozen Task4F1 measurement. Muse subscription only. Set threads=1 and PYTHONDONTWRITEBYTECODE=1 before source imports. /home/mdp/muse-work/ml-python -B has numpy (Python3.14.4/numpy2.5.3). Write STATUS.md first. Target a bounded ~20 minute first pass with early evidence; partial/blocker better than fabrication.

Source roots:
W=/mnt/c/Users/MDP/dev/llmzip-work
T=W/theory_benchmark_test_v1 : PLAN.md plus {lme,locomo,realtalk,perltqa}/{REPORT.md,COORDINATOR_REVIEW.md,COORDINATOR_AGGREGATE.json,summary.json,gate.json,per_query.jsonl,runner scripts}.
M=W/math_discovery_2026_09_13 : round3/all_n_ranking/{REPORT.md,COORDINATOR_REVIEW.md}, sign_mechanism/{REPORT.md,COORDINATOR_REVIEW.md}, round3/joint_gold_bounds/..., round2/allocation_optimality/... . Read only relevant files; coordinator corrections supersede unreviewed worker prose.

Known REAL observed LOW48 mean endpoint contrasts (sign-float at scale4 minus scale.25), verified from raw tables: LME -6.44326pp (nominalCI[-9.2734,-3.6871]), REALTALK -.093606pp (CI[-1.9330,+1.8490]), PerLTQA +2.46415pp (CI[1.5540,3.44284]); LoCoMo -2.58291pp (CI[-3.77676,-1.31321]) CONDITIONAL because float historical gate not met. Existing outcomes already seen: all further diagnosis is post-hoc, not preregistered or a held-out prediction. Four primaries unadjusted; no pooling/familywise claims. Positive diagonal scale makes SIGN invariant by construction, so outcome is entirely change in float scoring. HIGH48(1/t) equals LOW48(t) up to a positive global scale and gives no independent evidence. This is not a new compression method.

Deliver REPORT.md (plain-English or Turkish summary + technical evidence), verify.py, results.json, STATUS.md and any required independent checker. Every narrative file starts [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. Record source file+line for claims; hash source bytes read before/after. Separate algebra error, wrong assumptions, unidentifiable mapping, metric mismatch, numerical bugs, inference overreach. Don't mark 'proven cause' from correlation. Counterexample-first, exact rational synthetic examples, at least one real executable check when required. No 'independent review' claim from two code paths by you. Preserve evidence, don't edit old artifacts. Finish with a ranked error ledger (claim -> evidence -> correction -> still valid -> minimal discriminating next test), exact executed commands and counts.

PRIMARY JOB: independent theorem-to-intervention fidelity audit.
Read Model H all-N proof and shared benchmark PLAN, then representative intervention code (LME run_lme.py; REALTALK transfer_runner.py; PerLTQA runner.py; LoCoMo run_locomo.py) before interpreting outcomes. In H, q=(1,1,1) is fixed as t changes documents; documents all share equal norm. Benchmark multiplies BOTH q and document LOW48 coords. Are we testing the same mathematical parameter? Show algebra exactly; identify which H assumptions are necessary for proof versus merely enough for synthetic setup, and which real-data mappings were never justified. Query magnitude alone does not establish relevance/nuisance; do not define signal using future gold after outcomes.
For arbitrary document C_i, query q and fixed group G, derive the TRUE pairwise ranking function under joint scaling. Let complement/group dot contributions a_i,b_i and squared document norms u_i,v_i. After removing only a COMMON positive query norm, derive score (a_i+t^2 b_i)/sqrt(u_i+t^2 v_i). Check sign/zero assumptions, distinguish raw cosine derivative from a rank-equivalent score derivative. Derive pairwise crossing conditions as function z=t^2, preserving sign constraints before squaring. Determine when uniform monotonicity is provable and give a minimal rational counterexample when it isn't; don't propose a universal rule without necessary qualifications.
Compare document-only, query-only, joint scaling and different document norms with MATCHED algebraic ablations (synthetic only; no new empirical arms). Show whether nonmonotonicity is possible even with fixed group and shared query. Audit overclaim 'math theorem remains true' independently too: if statement/proof wrong, say so. Conclude which error is ours (experimental identification) versus a genuine theorem error. Scope no broad benchmark regression or saved data fitting.