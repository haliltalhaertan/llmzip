Focused third-pass mathematics for llmzip. Work ONLY in current /home/mdp/muse-work/math3-* directory. No benchmark runs, no network/web/install, no external model APIs, no git push/main changes. All prior source files are read-only. Use Muse subscription only. Bounded ~15 minute pass: first write a short STATUS.md immediately (before reading large sources); then one theorem or honest counterexample, runnable verify.py and results.json, REPORT.md. Keep input reading narrow. Prior two conditional-ranking sessions failed before producing files due to 180s model-stream idle timeout; do not assume a mathematical failure or existing partial proof.

Every narrative output: [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. Claim no scientific priority, universal security/losslessness or new benchmark win. Use explicit quantifiers, tie laws, assumptions, exact Fractions. Proofs, finite exhaustive witnesses and conjectures are distinct. Run own checker AND a differently formulated checker/hand derivation, including a false-claim negative control. If no new result, say so. Persist failed attempts, final scope limits, commands/exit codes. Final response concise.

Outputs to current workspace only: STATUS.md, REPORT.md, verify.py, results.json (+ optional independent checker). No broad literature/report sweep. No self-imposed expansion beyond the focused target.

TARGET: EXACT JOINT MULTI-GOLD BOUNDS, not averaging incompatible per-gold maxima.
Read ONLY /mnt/c/Users/MDP/dev/llmzip-work/math_discovery_2026_09_13/round2/sharp_bounds/{COORDINATOR_REVIEW.md,REPORT.md} initially.
Known valid premise: binary codebook, duplicates allowed, fixed retained set S size s of b, r=b-s, only full query distances d_i known. Retained distances e_i may be chosen independently in integer intervals [max(0,d_i-r),min(s,d_i)]; every such product choice jointly realizable. Metric = expected fraction of the m designated gold documents in topK under uniform random tie priority. K=3 of special interest, general 1<=K<=n.
Previous worker called exact joint optimization open, but coordinator suggests a simple solution: for fixed priorities, moving any GOLD earlier cannot reduce number of golds in topK (at worst displaces gold); moving any nongold later similarly cannot hurt. Thus candidate exact joint maximum: ALL golds at L, ALL nongolds at U; minimum reverse. Prove or refute rigorously; handle simultaneous ties, m>1, K>=m, K=n, s=0/r=0. Sharp witnesses via interval realization. This is NOT same as sum individual maxima. Reproduce prior example d=(2,2,0,0), b=4,s=2,K=3,golds={0,1}: independent maxima avg1 but joint max3/4.
If corner theorem holds, derive efficient exact evaluation (tie bucket counts, expected weighted/unweighted gold count) and full-case best/worst bounds on recall CHANGE relative to original code. Quantify whether nonnegative gold weights preserve corner theorem; don't assume moving a low-weight gold before high-weight gold is harmless. Seek smallest weighted counterexample if not. Distinguish per-question fractional recall from macro/micro benchmark aggregation; no empirical run.
Verification: bounded exhaustive enumeration all interval choices for small profiles/multiple gold sets at K=3 and edge cases, exact rational tie expectation; compare direct ranking-permutation formulation or explicit codebook construction. False variant = average individual extrema universally sharp, must reject. Avoid unnecessary DP/hardness claims. One rigorous extension is enough.