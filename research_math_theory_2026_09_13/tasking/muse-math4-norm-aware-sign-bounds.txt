Fourth focused math pass, llmzip. Seek ONE rigorous useful result, not optimistic prose. Current workspace ONLY under /home/mdp/muse-work/math4-*. All old sources read-only; no benchmark sweeps/new trained arms, no Git push/main edits, no Task4F1 measurement, no installs/network/model APIs. Muse subscription only. Write STATUS.md immediately, persist results early. ~20 minute first-pass target, report honest incomplete status if proof/implementation not finished. No infinite random searches. Python stdlib preferred, /home/mdp/muse-work/ml-python -B for numpy; threads=1 and no-bytecode.

Narrow mandatory context:
W=/mnt/c/Users/MDP/dev/llmzip-work
W/theory_benchmark_test_v1/audit_mapping/{COORDINATOR_REVIEW.md,REPORT.md}
W/theory_benchmark_test_v1/audit_real_geometry/{COORDINATOR_REVIEW.md,COORDINATOR_LME_WITNESS.json}
W/math_discovery_2026_09_13/round3/joint_gold_bounds/COORDINATOR_REVIEW.md if discussing joint bounds.
Corrections govern worker prose. Real witness LME15745da0 gold56/rival368 at t4: gold numerator1.02997959496 < rival1.04672119662 yet cosine .54573812 > .53902568 because norms differ. Numerator alone does NOT explain the flip. t versus t^2 alone does not explain opposite Model-H dominance; monotone reparameterization retains its crossover in the exact toy. Query magnitude is NOT a proved nuisance label. Current +12pp LoCoMo claim is retracted; current same-cache MC difference+6.82838pp only. No literature novelty claim.

Output REPORT.md, verify.py, results.json, STATUS.md; optional separate checker. Labels on narrative: [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. Full assumptions/quantifiers, exact ties and score definitions; separate theorem, finite witness, conjecture and real-data illustration. Proof arithmetic audited independently (different formulation); a seeded false/broken variant must fail FOR THE INTENDED REASON. Beware green checks that encode the same mistake. Save counterexamples and limitations, no silent source rewrites. Final answer contains exact commands/results and concise scope.

TASK: NORMALIZATION-AWARE SIGN-CODE UNCERTAINTY AND RANKING CERTIFICATE.
Can sign bits alone guarantee cosine ordering under arbitrary magnitudes? Work with normalized unit doc x and fixed full-precision unit query q, sign pattern s and Hamming score; then separately identify what changes when query also sign-coded as in frozen native protocol. No assumption all magnitudes equal. Seek exact best/worst cosine compatible with a sign pattern (positive/negative orthant on sphere) and optional limited side information (e.g. per-document magnitude-concentration/radius). Handle sign(x)>=0 convention: negative signs require strictly negative coordinates while positive signs include zero, so sup/inf may not be attained; do not falsely claim closed compact feasible sets. Derive sharp extrema or certified bounds and explicit witnesses showing sign-only information can be insufficient.
Then investigate ONE scalar that meaningfully tightens a deterministic score bound (angle between unit x and sign(s)/sqrt(d), or residual norm), derive correct normalized pairwise/topK margin certificate. Both doc and query approximations may share uncertainty; avoid pretending independent extrema jointly attainable. Explain what can be computed at encoding time without gold labels and what query-side information is assumed. Treat any new scalar as EXTRA PAYLOAD/shared metadata with explicit bit precision, conservative rounding/error interval; do NOT claim a free 12-byte method or run unapproved new benchmark codec.
Compare against elementary Cauchy-Schwarz/angle bounds: if you only recover a known inequality, say so, don't rename it a discovery. Seek an explicit case where scalar side information turns an otherwise vacuous sign-only bound into a certified top3 decision, plus a failure case. Exact rational small examples and exhaustive checks with correct zero/open-boundary semantics; independent algebraic/geometric validation. If practical bound too loose, an honest quantified negative result is useful. No learned selector, no fitting post-hoc to gold, no transfer from inner-product preservation to relevance superiority.