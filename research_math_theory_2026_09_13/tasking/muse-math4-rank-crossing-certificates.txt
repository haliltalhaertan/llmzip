Fourth focused math pass, llmzip. Seek ONE rigorous useful result, not optimistic prose. Current workspace ONLY under /home/mdp/muse-work/math4-*. All old sources read-only; no benchmark sweeps/new trained arms, no Git push/main edits, no Task4F1 measurement, no installs/network/model APIs. Muse subscription only. Write STATUS.md immediately, persist results early. ~20 minute first-pass target, report honest incomplete status if proof/implementation not finished. No infinite random searches. Python stdlib preferred, /home/mdp/muse-work/ml-python -B for numpy; threads=1 and no-bytecode.

Narrow mandatory context:
W=/mnt/c/Users/MDP/dev/llmzip-work
W/theory_benchmark_test_v1/audit_mapping/{COORDINATOR_REVIEW.md,REPORT.md}
W/theory_benchmark_test_v1/audit_real_geometry/{COORDINATOR_REVIEW.md,COORDINATOR_LME_WITNESS.json}
W/math_discovery_2026_09_13/round3/joint_gold_bounds/COORDINATOR_REVIEW.md if discussing joint bounds.
Corrections govern worker prose. Real witness LME15745da0 gold56/rival368 at t4: gold numerator1.02997959496 < rival1.04672119662 yet cosine .54573812 > .53902568 because norms differ. Numerator alone does NOT explain the flip. t versus t^2 alone does not explain opposite Model-H dominance; monotone reparameterization retains its crossover in the exact toy. Query magnitude is NOT a proved nuisance label. Current +12pp LoCoMo claim is retracted; current same-cache MC difference+6.82838pp only. No literature novelty claim.

Output REPORT.md, verify.py, results.json, STATUS.md; optional separate checker. Labels on narrative: [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. Full assumptions/quantifiers, exact ties and score definitions; separate theorem, finite witness, conjecture and real-data illustration. Proof arithmetic audited independently (different formulation); a seeded false/broken variant must fail FOR THE INTENDED REASON. Beware green checks that encode the same mistake. Save counterexamples and limitations, no silent source rewrites. Final answer contains exact commands/results and concise scope.

TASK: EXACT RANK-CHANGE / TOP-K STABILITY CERTIFICATES UNDER THE ACTUAL NORMALIZED SCORING LAW.
For fixed coordinate group G, both doc/query scaled by t>0, let z=t^2 and remove common positive query norm. Per-doc rank score f_i(z)=(a_i+b_i*z)/sqrt(u_i+v_i*z); doc/query are arbitrary, not equal-norm. Domain u_i+v_i*z>0. Derive pairwise equality polynomial P_ij(z)=(a_i+b_i*z)^2(u_j+v_j*z)-(a_j+b_j*z)^2(u_i+v_i*z), but preserve numerator signs and exact-zero cases: squaring adds spurious roots. Handle identically-zero polynomial (persistent ties/opposite signs), roots shared with numerator zeros, multiple/tangent roots, s=0/r=0 analogues and half-open interval boundary semantics. A root may be a touch, not order reversal.
Prove ranking constant on intervals between valid crossing events; derive sufficient/necessary topK SET stability criterion checking inside/outside pairs, with explicit boundary ties. For deterministic fixed priorities versus expected-tie recall state separate objects. Give a certified finite procedure for rational coefficients on a bounded positive interval (e.g. [1/16,16]); use exact polynomial/Sturm/root-isolation if feasible, otherwise honest conservative interval certificate with UNRESOLVED output, never call numpy roots certified. Bound complexity without treating all pair roots as topK changes. Distinguish exact rational interpretation of stored float coefficients from guarantees about floating BLAS evaluation.
Verification: exact rational constructed cases including prior 2+2 nonmonotone witness (a,b,u,v)=(-3,4,5,10) vs(-2,2,2,10), persistent ties, opposite-sign fake roots, tangent roots. Try fewer-dim counterexample only within bounded budget; no minimality claim unless proved. OPTIONAL bounded illustration on the one original LME witness cache W/regen/lme/cache_repr/15745da0.pkl; if used, mark post-hoc explanatory and compare direct cosine signs at bracket endpoints, not out-of-sample prediction. At least one computational artifact must run; no new full benchmark run. Practical output could be a certified safe t interval, not a new codec.