Focused third-pass mathematics for llmzip. Work ONLY in current /home/mdp/muse-work/math3-* directory. No benchmark runs, no network/web/install, no external model APIs, no git push/main changes. All prior source files are read-only. Use Muse subscription only. Bounded ~15 minute pass: first write a short STATUS.md immediately (before reading large sources); then one theorem or honest counterexample, runnable verify.py and results.json, REPORT.md. Keep input reading narrow. Prior two conditional-ranking sessions failed before producing files due to 180s model-stream idle timeout; do not assume a mathematical failure or existing partial proof.

Every narrative output: [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]. Claim no scientific priority, universal security/losslessness or new benchmark win. Use explicit quantifiers, tie laws, assumptions, exact Fractions. Proofs, finite exhaustive witnesses and conjectures are distinct. Run own checker AND a differently formulated checker/hand derivation, including a false-claim negative control. If no new result, say so. Persist failed attempts, final scope limits, commands/exit codes. Final response concise.

Outputs to current workspace only: STATUS.md, REPORT.md, verify.py, results.json (+ optional independent checker). No broad literature/report sweep. No self-imposed expansion beyond the focused target.

TARGET: ALL-N/K DOMINANCE FOR CORRECTED SHARED-GOLD SIGN MODEL.
Read ONLY /mnt/c/Users/MDP/dev/llmzip-work/math_discovery_2026_09_13/sign_mechanism/{COORDINATOR_REVIEW.md,check_conditional_transport.py,conditional_transport_results.json} initially; original REPORT.md only if needed.
Synthetic model H: q=(1,1,1), one gold R=(1,t eps1,t eps2), N-1 iid nongolds I_j=(-1,t delta_j1,t delta_j2), each nuisance sign iid +-1, SAME randomly drawn R shared by all comparisons. t>0. Sign = nonnegative bits + Hamming; cosine normalized (all docs share norm sqrt(1+2t^2), so within this model dot order equivalent). topK uniform tie priorities. Do NOT use unconditional pair probabilities as iid: condition on shared gold then average. Correct values at N6 K3: sign1763/2048, cosine(t=.5)4067/4096, cosine(t=10)1483/2048.
Potential simplification: let A count gold nuisance agreement, B_j count nongold agreement. Compare pairwise sign distance gap and normalized-cosine ordering as functions of integer B_j-A. Classify ALL t intervals and boundaries exactly. Use SAME document priorities for coupling: if each nongold can only move from ahead->tied->behind relative to gold as t changes, topK inclusion monotonicity may prove dominance for EVERY archive realization and thus all N,K, not merely an unconditional pairwise advantage. Try to prove this, and determine where inequalities are strict vs equality/degenerate K. This is a more focused replacement for the timed-out broader task.
Derive conditional finite sum if helpful, but don't let brute-force N sweeps stand in for universal proof. Check N small exact enumeration with same shared gold and all nuisance assignments, K3 and general K, plus thresholds t=1/2 and t=1 and representatives around them. Confirm corrected N6/N10 coordinator results through an independent exact computation. False control = original unconditional multinomial, should mismatch at least one known case.
Do not imply this toy models true benchmark dependence/centering. Its sign-vs-cosine direction may be very special. One clean parametric theorem with scope is sufficient; avoid speculative literature/causal prose.