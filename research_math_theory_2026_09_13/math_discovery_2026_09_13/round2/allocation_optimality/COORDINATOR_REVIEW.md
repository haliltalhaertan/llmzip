# Coordinator review: second-pass allocation optimality

[LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]

Executed worker verify.py and checker.py; both exit 0. All 17 named verify checks pass. Exact pair enumeration and gap convolution agree at 14 allocation nodes; tampered optimum table is rejected. These are multiple worker-authored implementations rerun by the coordinator, not an independent institution or formal kernel proof.

Supported finite witness: in the stated independent-coordinate synthetic law and asymmetric full-query inner-product scoring, all three greedy bit choices have unique positive best immediate gain. Greedy allocates (1,1,1) with conditional pair-order error 117/718, but complete budget-three enumeration has unique optimum (2,1,0) with error 41/718. The distinction is not arbitrary tie-breaking of greedy choices. This refutes general greedy ranking-error optimality within this model; it says nothing quantitative about frozen benchmark relevance recall.

The separable-MSE exchange argument supplies a SUFFICIENT condition: nonnegative diminishing marginal gains with prefix-feasible bit choices. The final worker stdout says 'iff', which overstates the actual theorem: diminishing returns need not be necessary for greedy to succeed on a particular instance. Use 'sufficient', not 'if and only if'. Both abstract and quantizer-realizable counterexamples show separability alone is insufficient.

Shared-cost descriptions are schematic and use inconsistent counting conventions for symmetric reconstruction levels across allocations. No actual serialization or total-byte theorem exists. Do not quote the six-versus-three level-value counts as minimal storage.

The report line 'per-pair MV allocation optimizes a union-bound surrogate' is not established; selecting one-step pairwise improvements does not guarantee the global optimum of the aggregate bound. Earlier caveats about crossing events and no top-K transfer still apply.

No literature-priority claim; no empirical deployment or frozen-data rerun. Original report preserved for provenance with this qualification.
