Own only /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/audit. Read ../PROTOCOL.md, other directories READ ONLY. $HOME/muse-work/ml-python. No paid APIs/git. Four producer jobs baseline,data,pq,semantic running; data done. Independent audit and corrections, NOT rubber stamp. FIRST known bug: data/run_lexical.py expected_hit() calculates fractional expected Recall@10, NOT any-gold probability Hit@10 (REPORT falsely claims those equal). Keep historical files unchanged; create corrected lexical per_query and supplementary report in audit, test counterexample multi-gold cutoff/ties (RED on producer then GREEN on independent replacement, mutation guard). Correct expectedHit formula: if any gold strictly above cutoff ->1; else 1-C(B-G,s)/C(B,s) from boundary bucket B/G and available slots s; if no gold inboundary ->0. Actual Hit10/Recall10 top10 may still correct. Independently reimplement BM25 + TFIDF all705, rank protocol SHA tie, assert top10 exactly matches original, actualmetrics correct, save expectedHit corrected. Compute true boundary ties; no assumption float meansno ties. Assert allsources/exports align cachedquestion/gold and rawdoc text; emptygold23 original; check qa_diag unresolved directly (matching cachedgold alone doesn'tprove no unresolved!). Record prior/source hashes before/after under own dir; no prior edits.
SECOND when baseline/pq artifacts available audit full perquery top10 ranking+actualmetrics from original caches and serializedcodes, not summary only. All9440 forbaseline and705PQ; can import model-independent utilities only as crosscheck, own ranking/metrics necessary. Check REALTALK armcounts exact; historicalFR3 replay statuses verify sourcecode not only tests count. Identify decoder/codebookcost/floatdtype allocation problems; binsign >=0 true vs int8-round0. Analyze equalpayload vs totalstorage.
THIRD inspect semantic source code when written for pooling/INT8/binary fidelity to official st_quantize.py, attention bidirectionality, batchsorting preservation, docs-onlyPCAfit and footprint. Do NOT runfullmodel concurrently (memory/CPUcontention); save review for coordinator, outputs may unfinished ->explicitpending not fabricate. Get as far as possible within15minutes with audit reports saved aftereachsubtask. No polling loop unless brief bounded dependency wait; focus data thenbaseline/PQ existing files. Finish check code by unittest harness actual output. If producer not complete reportpending handles, coordinator will rerun auditafter. Write AUDIT_REPORT.md, VERIFIED.json with verified statusperarm/dataset and actual counts, timings, repairedlexical JSONL. Four labels. You may write a reusable audit driver coordinator can rerun aftersemanticcomplete. Do not tune or alter producer rankings.