You own /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/pq only; read ../PROTOCOL.md. Use $HOME/muse-work/ml-python, faiss installed. No paid APIs, no changes to existing input files/git. Task actual equal12byte conventional PQ comparison for REALTALK705 against ourSIGN96. Archives cached in /mnt/c/Users/MDP/dev/llmzip-work/bench3/runs/b3a_realtalk/rt_repr/RT*.pkl. Train Faiss ProductQuantizer96dims,M12,nbits8 once on allREAL DOCUMENTS ONLY (global across10archives, NO query/gold), cp.seed20260915 cp.niter20, faiss.omp_set_num_threads(1). Existing FLOAT96 C vectors must L2normalize before training/coding; asymmetric distance original normalized QC to reconstructed code vectors L2 (not normalize reconstructed to silently changeL2ADC). Exactly12 bytes each code, codebook98,304bytes expected report actual; use centroid LUT ADC without full document reconstruction at query for primary scorer. Score=-sum per-subspace squareddistance. Test LUT scores against full reconstructed L2 brute force1e-5, packed code shape, bitroundtrip, no leaked training queries, top10 determinism/ties including gold atcutoff, all705 canonical gold. Primary tie SHA256('top10-r1|'+RTxx+'|'+str(row)) ascending hash, thenrow; allhigher scores better. Implement TDD RED expected fail thenGREEN perfeature save logs. Save centroids.npy, each archive codes.npz with qC and gold binding/inputsha; per_query.jsonl top10 IDs/gold/Hit10/Recall10/nDCG10; SUMMARY.json, REPORT.md with four labels. Cannot read other workers' result scores to tune. No hyperparameter search. Do not claim equal TOTAL memory from equal12B doccodes. Report total docs/query counts, codebook amortization, encoder shared state missing fromoldcache NOT MEASURED, build/query timing and RSS. Also independently audit comparison protocol risks and frozen gold semantics from adapter; note cluster10limits. Run and finish, not just propose. Max15min.