Work on llmzip Top10 comparison. Read /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/PROTOCOL.md first and follow exactly. Use $HOME/muse-work/ml-python (numpy sklearn faiss torch transformers available). No paid APIs, no web tools, no input changes, no git writes. Work only in assigned output directory. Implement strict vertical TDD: failing feature assertion first, minimal implementation, green, then next; retain logs. Write full reproducible code, per-query results and REPORT.md with four protocol labels. Do not invent missing inputs; report blockers. Existing prior source /mnt/c/Users/MDP/dev/llmzip-work/parallel_ideas_r1/asymmetric/run.py contains cache paths, loaders, historical replay gates, standardized cosine definition; read/reuse as library only without invoking its write-producing main. Old per-query comparator /theory_benchmark_test_v1/{perltqa,lme,realtalk}/per_query.jsonl. All absolute paths under /mnt/c/Users/MDP/dev/llmzip-work. Limit your job to20minutes with useful artifacts at checkpoints. OWN directory top10_comparison_r1/baseline. Implement and EXECUTE all four cached-representation arms for all9440queries (8265,470,705), new Hit10/Recall10/nDCG10 + expected tieHit10. Per-query store qid,archive_id,gold,row count,top10 IDs,scores/tie metadata. Historical gates for SIGN/FLOAT FR3 must match all original per-query. Derive own metric implementation with tests incl brute-force random-tie permutations for small fixtures. Save score arrays or source bindings to enable independent replay. Report bytes/timing; all queries no truncation. You do NOT own raw data export or model. Main report separate datasets, not average.