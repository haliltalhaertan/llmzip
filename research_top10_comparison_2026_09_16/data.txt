Work on llmzip Top10 comparison. Read /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/PROTOCOL.md first and follow exactly. Use $HOME/muse-work/ml-python (numpy sklearn faiss torch transformers available). No paid APIs, no web tools, no input changes, no git writes. Work only in assigned output directory. Implement strict vertical TDD: failing feature assertion first, minimal implementation, green, then next; retain logs. Write full reproducible code, per-query results and REPORT.md with four protocol labels. Do not invent missing inputs; report blockers. Existing prior source /mnt/c/Users/MDP/dev/llmzip-work/parallel_ideas_r1/asymmetric/run.py contains cache paths, loaders, historical replay gates, standardized cosine definition; read/reuse as library only without invoking its write-producing main. Old per-query comparator /theory_benchmark_test_v1/{perltqa,lme,realtalk}/per_query.jsonl. All absolute paths under /mnt/c/Users/MDP/dev/llmzip-work. Limit your job to20minutes with useful artifacts at checkpoints. OWN directory top10_comparison_r1/data. Export complete REALTALK canonical text/docs/query/gold aligned exactly to existing10 cache files. Read bench3/runs/b3a_realtalk/bench3_realtalk_adapter.py; use cache['file'] to rawChat file, frozen message_text speaker/clean_text/image format; not date; no event annotations. Verify EVERY dia_id to cached id_to_row, every question exact, every cached gold stable (also recompute via old normalizer to detect mismatch). Output per-archive RTxx.json schema {archive_id,source_file,docs:[{row,id,text}],queries:[{qid,text,gold:[rowints],category}]} include only valid705 across10; separately exclusions.json. Preserve row IDs. Raw gold/answers not included in embedding texts. manifest.json count+hash of all sources and exports. Also implement/execute BM25 and TFIDF lexical controls on SAME705, deterministic protocol tie rule; per_query.jsonl top10, hits,recall etc; no model downloads. Signal export completion with EXPORT_DONE.json BEFORE running lexical controls, so another worker can consume files. Complete export first within5min if possible; no old TFIDF/SVD refit.