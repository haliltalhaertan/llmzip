You own /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/semantic only. Work on actual literature-candidate comparison; read ../PROTOCOL.md, ../MODEL_README.md and ../official_st_quantize.py. Local CPU ONLY no paid APIs/git/input changes. Use $HOME/muse-work/ml-python (torch2.14 CPU, transformers installed, NOT sentence_transformers). Coordinator downloads pinned model to ../model and writes ../MODEL_DOWNLOAD.json. Dataset worker exports ../data/RTxx.json schema {archive_id,source_file,docs:[{row,id,text}],queries:[{qid,text,gold:[rowints],category}]} and ../data/EXPORT_DONE.json; can inspect progress/files without altering them. All10 archives, all705 valid queries (728-23), complete corpus. Task: implement TDD encoder and ranking tests then RUN PPLX0.6b inference and score all705. You have max30min initial; save checkpoint perbatch and perarchive, do not claim completion unless all.
Important official semantics: AutoModel remote modeling code downloaded locally; inspect it for side effects and dependencies BEFORE trust_remote_code. Likely mean pool hidden states using attentionmask then round(127*tanh(pooled)), clip[-128,127]. Native BINARY = sign of UNQUANTIZED pooled activations >=0, NOT sign of rounded INT8 (negative small values round0!). Save float pooled values as f32, true packed sign and true native INT8 arrays, compare exact with official quantizer formulas fixtures. SentenceTransformers unavailable; use AutoTokenizer and AutoModel(local directory, trust_remote_code=True, local_files_only=True) with faithful manual official masked mean pooling/config. Do NOT install unreviewed code or alter architecture to make it run. Read ../model/modules.json and 1_Pooling/config.json first. Decoder has custom bidirectional model code. CPU16threads available but use torch.set_num_threads(6), interop1; float32 weights. Batch lengths sort (preserve original rows!) max1024, lengthbucket batches4-8, inference_mode; report exact trunc counts tokenizer untruncated lengths and empty strings. Save output after each batch; resume safe pertext hashes, avoid reencoding. Report first batch speed and estimated time in progress.json. Stop only if hard memory/runtime failure, produce blocker; no synthetic embeddings. Could use continuous pooled cosine as additional arm label PREQUANT_POOLED not paper baseline; required INT8 cosine, trueBIN Hamming, INT8 query x trueBIN docs; protocol ties ascending SHA256('top10-r1|'+archive_id+'|'+str(row)), then row.
Run declared PCA96 on globally concatenated REALTALK documents INT8(f32) ONLY random_state20260915, no query/gold fit, center via saved mean, sign(projected) and pack12B. Do after all10 encoded; save projector and mean bytes. No support/native MRL claim.
Per-query JSONL each qid archive_id gold scores/top10 for allsemantic arms and metrics Hit10 Recall10 nDCG10; payload NPZ docs/query arrays allow independent replay. peakRSS via resource if no psutil; total encoder weight bytes, build/run timings/candidate scoring time, allshared payloads explicitly. Save REPORT.md and SUMMARY.json four labels. Test packed roundtrip, nativequantization nearzero counterexample, lengthbatch reorder, no gold leakage, ties and exact10. Save RED/GREEN logs. Do not wait doing nothing while dependencies download: inspect code and write/tests first. If process ends while inference running independently, print process PID exact launch/log/checkpoints. Prefer stay until finished within1800s and preserve all artifacts.