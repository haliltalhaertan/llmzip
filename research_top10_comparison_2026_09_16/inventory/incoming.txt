You are a research INVENTORY agent for the llmzip programme. Goal: catalogue what is ALREADY
MEASURED so the team does not repeat experiments ("do not rediscover America"). You are NOT asked to
invent new methods or run new benchmarks.

HARD RULES
- Use $HOME/muse-work/ml-python for any python (numpy/sklearn/faiss/torch available). No pip installs.
- NO paid APIs, NO web tools, NO git commands, NO writes outside YOUR OWN directory (given below).
- Never modify any input file. Treat everything outside your directory as READ-ONLY.
- Label every output with: [LOCAL EXPLORATORY PILOT] [NOT PREREGISTERED] [NOT FOR CITATION] [DISCLOSE-BEFORE-USE]
- Distinguish three statuses for EVERY number you record, and never upgrade a status:
    VERIFIED   = you re-derived it yourself from raw per-query data this session
    CLAIM      = a producer report states it; you did not re-derive it
    RELAYED    = someone quotes someone else; original not inspected
- Do not average across benchmarks. LME / PerLTQA / RealTalk / LoCoMo are separate.
- If a file is missing or unreadable, write it down as a gap. Do NOT invent contents.
- Budget: ~20 minutes. Save partial artifacts as you go; a partial honest report beats a late perfect one.
- Report facts, not encouragement. If the evidence is weak, say so plainly.

ALREADY DONE BY THE COORDINATOR (do not redo, but you may build on it):
- Our own independent baseline exists at /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/baseline/
  per_query_top10.jsonl: 9440 queries, arms sign96/float_raw/float_std/asym, Hit@10 + Recall@10 +
  nDCG@10 + exact expected-Hit@10 under uniform ties, plus a historical FR@3 replay gate that PASSED
  (max deviation 0.0, tolerance 1e-12) on all three benchmarks.
- Coordinator already cross-checked the incoming 2026-09-16 package with independent code
  (/mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/coordinator/verify_incoming.py, results in
  verify_incoming.json): 12/12 old-arm cells matched EXACTLY (0.000000 pp), and their NEW headline arm
  "qscale" reproduced EXACTLY on all three benchmarks (LME 88.5106, PerLTQA 80.0000, RealTalk 49.6454).
  One arm did NOT match: "qsign_dstd" deviated by LME +0.6383, PerLTQA -0.2662, RealTalk -0.8511 pp.

YOUR DIRECTORY (write ONLY here): /mnt/c/Users/MDP/dev/llmzip-work/top10_comparison_r1/inventory/incoming
YOUR JOB: deep-read the incoming 2026-09-16 package (produced by another LLM, gpt-6-pro) down to its raw
data and source code, and separate what is genuinely NEW from what we already had.

EXTRACTED PACKAGES (read-only):
/mnt/c/Users/MDP/dev/llmzip-work/incoming_20260916/extracted/
  - LLMZIP_HATA_YERI_2026-09-16/        (stage/readout diagnostic; readout_diagnostic.py, stage_diagnostic.py,
                                         results/readout_per_query.csv, results/raw_stage_per_query.csv,
                                         results/readout_summary.json, results/scale_diagnostics.json,
                                         results/DIAGNOSIS_SUMMARY.json, results/exploratory_cluster_bootstrap.json)
  - LLMZIP_HIZ_GENELLEME_2026-09-16/    (speed + LoCoMo transfer; scripts/kernels.c, scripts/native_scorers.py,
                                         scripts/core.py, PLAN_BEFORE_RUN.json, results/FINAL_SUMMARY.json,
                                         results/quality_per_query.csv, results/locomo_paired_comparisons.json,
                                         results/optimized_speed.json, results/end_to_end.json)
  - LLMZIP_DENETIM_INCELEMESI_2026-09-16/ (their counter-review of an adversarial audit; check_ndcg_ties.py,
                                         paired_metric_differences.csv, ndcg_exact_checks.json)
  - LLMZIP_BENCHMARK_2026-09-15/        (earlier benchmark; scripts/quality.py, scripts/common.py,
                                         results/quality_per_query.csv, results/SUMMARY.json)

TASKS, in priority order:
1. RESOLVE THE ONE DISCREPANCY. The coordinator reproduced their `qscale` arm EXACTLY but their
   `query_sign_doc_std` arm ("qsign_dstd") deviated (LME +0.6383, PerLTQA -0.2662, RealTalk -0.8511 pp).
   Coordinator's definition was: score = sum_j (C_dj / sigma_j) * b_qj, with b_q = +1 if qC_j >= 0 else -1,
   sigma = per-archive std of DOCUMENT C columns (ddof=0, floor 1e-12). Read THEIR actual implementation
   in readout_diagnostic.py (and scripts/quality.py) and report the exact definitional difference.
   Say plainly which definition each side used. Do not guess: quote the lines.
2. Verify their headline numbers from THEIR OWN raw per-query CSVs by re-aggregating yourself
   (readout_per_query.csv, quality_per_query.csv). Confirm or refute: the per-dataset Hit@10 table, the
   qscale-minus-Hamming deltas (LME +2.43 / PerLTQA +4.24 / RealTalk +2.96), and the LoCoMo numbers
   (Hamming 52.64 / asym 56.83 / new 57.22 / float 61.01, n=1531).
3. CRITICAL COMPARISON: their reported `doc_sign_query_raw` (old asym) vs their `doc_sign_query_std`
   (qscale). Compute qscale-minus-asym per benchmark from their raw data. The coordinator found this
   gap is very uneven (LME +2.77, PerLTQA -0.19, RealTalk +6.10 using OUR data). Confirm from theirs.
   This decides whether their "scale balancing" adds anything over the asymmetric scoring we ALREADY had.
4. Inspect their LoCoMo adapter (results/locomo_adapter_before_scores.json + the script that built it) and
   state exactly what cohort/exclusions were used (they claim 1531 of 1986, 446 adversarial cat-5 excluded).
   Flag anything that would make LoCoMo non-comparable to our other benchmarks.
5. List their claims that CANNOT be checked from what they shipped (e.g. AVX-512 timings need their CPU).

DELIVERABLES: `INCOMING_AUDIT.md` (findings, each marked VERIFIED/CLAIM/RELAYED with the file+line you used),
`recheck.py` (your re-aggregation code), `recheck_results.json`, and `NEW_VS_KNOWN.md` listing every element
of their package as either GENUINELY-NEW, ALREADY-HAD (with our file that already has it), or CONTRADICTS-OURS.
